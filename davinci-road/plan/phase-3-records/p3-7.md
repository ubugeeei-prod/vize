# P3-7 - VDOM patch facts

P3-7 closed on 2026-09-16. VDOM patch decisions use the owner-keyed S2-to-S4
fact surface, with binding metadata projected through the S3 lattice. The
hydrated DOM corpus verifies byte parity and actual table materialization.

## Boundary

The slice chooses the S2 to S4 annotation route for now. Current S3 records the
canonical op graph and static/dynamic partition, but it does not yet carry the
typed binding payloads that Vue patch flags need: static bind keys, modifier
realization, cached-handler state, component fallthrough props, and legacy
patchless runtime expressions. Sending DOM through S3 here would either lose
those inputs or re-derive them later, which is exactly the P3-7 problem.

The initial slice left lattice binding integration and the DOM corpus gate
open; the subsequent slices and terminal evidence below complete those steps.

The 2026-09-13 second slice materializes an owner-keyed `PatchFacts` side table
inside the S2 DOM emitter before VNode call assembly. Native and component
writers now read the table through `EmitCx::materialize_patch_facts`, while
position-local masks (`v-for`, `v-once`, `v-memo`, text children, slots, and
static refs) remain in the writer because their inputs are still emission
context rather than binding facts.

The 2026-09-13 third slice wires binding metadata through the S3 reactivity
lattice vocabulary before the patch-fact consumer asks whether a handler is
constant. The bridge keeps the shipped handler surface exact (`SetupConst` and
`LiteralConst` only) while representing `ExternalModule`, globals, props, refs,
data, options, and Vue globals as typed lattice facts for the next S3 hand-off.

The 2026-09-13 fourth slice makes that side table retain owner-keyed entries
instead of returning a transient calculation. Anonymous patch facts still pass
through without storage, and repeated materialization for the same owner replaces
the existing row so the table can become gate evidence without growing stale
duplicates.

The 2026-09-13 fifth slice adds a test-only DOM emit observation that records
the retained table row count after VNode emission. Patch-flag equivalence
fixtures now assert both byte-compatible patch sites and owner-keyed table
materialization, so bypassing the table is visible before the broader TS-11
corpus gate runs.

The 2026-09-16 sixth slice promotes that observation into the
`davinci-differential` corpus lane. The DOM corpus runner now emits
`patch_fact_entries` beside its byte-parity counts, the Real Project Matrix
artifact validator rejects zero-entry evidence, and the workflow summary
publishes the materialized owner-keyed row count. The post-merge run below
provides that terminal evidence on the commit containing the gate.

## Terminal corpus evidence

- Run: [Real Project Matrix 35055720056](https://github.com/ubugeeei-prod/vize/actions/runs/35055720056), completed successfully.
- Commit: `0e6985a4cb846e393daa1fad705b069e09bf85fa` (PR #6141).
- Job: [s2 dom corpus](https://github.com/ubugeeei-prod/vize/actions/runs/35055720056/job/104665376972), completed successfully.
- Artifact: `real-project-davinci-dom-corpus`, ID `10430093277`.
- Scope: 146 selected/hydrated gitlinks, 42,668 files parsed, 42,295 templates.
- Comparisons: 42,279; patch-fact entries: 390,264; S2 refusals: 0;
  divergences: 0; unreadable files: 0.
- `mode=enforce`, `verdict=success`, `canonicalScope=true`, `closureEvidence=true`.

Sixteen templates are skipped because the legacy compiler already reports
errors; `summary.json` records their diagnostic categories. This is parity
for the admitted DOM corpus, not a claim that those invalid templates compile.
Non-Davinci lint/typecheck/LSP surfaces in this isolated run used record-only
or skipped modes; their enforcement is outside this P3-7 evidence.

## Decisions

| subject       | decision                                                                                                                                                   |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Fact home     | `vize_s1_to_s2::emit::patch` owns `PatchFacts` instead of leaving the decision inside props emission.                                                      |
| Compatibility | Existing `vize_atelier_core::codegen::patch_flag` remains only for compatibility fallback lanes.                                                           |
| Data carried  | `PatchFacts` carries the numeric flag plus the dynamic-prop key list in source order.                                                                      |
| Table key     | `PatchFactsTable` is keyed by the owner `ui.element` / `ui.component` `NodeId` and populated before the patch flag or dynamic-props arguments are printed. |
| Lattice input | Binding metadata is projected into `vize_impeto::lattice::BindingInput` before patch facts consume constant-handler decisions.                             |
| Local masks   | `v-for`, `v-once`, `v-memo`, static refs, dynamic slots, and text-child flags stay at VNode position.                                                      |
| Parity rule   | The broad patch-flag fixtures still compare S2 DOM output against the shipped compatibility lane.                                                          |

## Mechanical witnesses

- `cargo check -p vize_s1_to_s2 -p vize_atelier_dom`
- `cargo test -p vize_atelier_dom --test davinci_s2_patch_flags -- --nocapture`
- `cargo test -p vize_atelier_dom --test davinci_s2_recent_patch_flags -- --nocapture`
- `cargo test -p vize_atelier_dom --test davinci_s2_patch_flags_static -- --nocapture`
- `cargo test -p vize_atelier_dom --test davinci_s2_patch_flags_legacy --features legacy -- --nocapture`
- `cargo test -p vize_s1_to_s2 emit::patch::tests --lib`
- `cargo test -p vize_s1_to_s2 --features davinci-differential --test davinci_dom_corpus`
- `node --test tests/tooling/davinci-patch-facts-table.test.ts`
- `node --test tests/tooling/davinci-dom-corpus-real-project-workflow.test.ts`

## Follow-up

Once S3 has typed binding payloads, this fact surface can
move from S2 to the explicit S3 decision stream without changing the VNode
writer again. The chosen S2-to-S4 route satisfies P3-7; the broader production
switch, other backend gates, and Phase 3 exit remain separate requirements.
