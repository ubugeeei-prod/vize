# P4-5b — template-expression anchors on the TS-40 matrix

The S4 projection (`project_template_expressions`) and the current
virtual-TS generator both run on every template in
`tests/_fixtures/davinci-ts40-projection/matrix.json` (12 fixtures).

Each S4 anchor must resolve to its own authored range. That range is
either an authored range of the current generator, or one opaque compound
interpolation (`{{` more than once) whose span contains the generator's
finer identifier ranges. Generated text is not compared. The comparison
count is pinned at 22. A zero run fails.

```
cargo test -p vize_canon --features davinci-differential --test davinci_projection_differential
```

P4-5b stays open. This run does not compare `vize check` diagnostic sets,
and it does not project script. The phase index is unchecked until that
corpus comparison is zero-divergence with scope proof.
