# P4-9b — Cross-file complexity rule and Doctor finding

**In progress (2026-09-22).** Slice 1 lands the Patina rule on **own**
complexity. Rendered (cross-file) complexity, the Doctor hotspot finding,
the facts-fed `ComplexityInput` and the removal of `logical_operator_count`
come in the following slices.

## Slice 1 — `vue/max-template-complexity`

| subject    | decision                                                                                                                                                                                                                                                      |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Home       | `crates/vize_patina/src/rules/facts/max_template_complexity.rs` (lane G), exported from the new `rules/facts.rs` and registered by one line in the opinionated bundle; lane B's `unused_setup_bindings` joins the same module.                                |
| Input      | `vize_s1_to_s2::pass::cfg::run_template_range` over the SFC descriptor's template range: file-absolute spans, no text scanning. `pug` and `src` templates are skipped (no S1 tree), never guessed.                                                            |
| Verdict    | Warning when own cyclomatic > 11 or own cognitive > 16, the full-corpus p95 pinned by P4-9a; the constants live beside the pass and a unit test holds them equal to the spec text.                                                                            |
| Tier       | `exact`: the rule states a property of facts proven against an independent evaluator (TS-34). Written in the rule docs; it enters the tier table when P4-6c creates it.                                                                                       |
| Output     | Primary span on the `<template>` tag, one label per top-five contributor (by cognitive increment, then cyclomatic, shown in source order), each naming its kind, nesting and both increments. The help text points at extraction, which never taxes a parent. |
| Preset     | `opinionated` only: a complexity budget is a convention, not a correctness check, so `happy-path` stays unchanged and TS-9's default-preset corpus runs do not move.                                                                                          |
| Descriptor | Added to `SHARED_SFC_DESCRIPTOR_RULES`, so the engine parses the SFC once for all descriptor rules.                                                                                                                                                           |

Mechanical witnesses: `cargo test -p vize_patina --lib max_template_complexity`
(the diagnostic compared exactly, the labels by message and span, the text
renderer snapshotted) and `cargo test -p vize_s1_to_s2 --lib cfg::source`.
The preset-membership snapshot, `npm/cli/src/types/rules.ts`, the rule-parity
matrix (249 rules; the rule is `container-bound`, `sfc-source`) and the rule
docs are updated with it.
