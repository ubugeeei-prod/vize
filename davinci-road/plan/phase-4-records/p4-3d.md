# P4-3d — Reactivity merges into the S3 lattice

**Checked (2026-09-23).** One lattice. `SourceKind::binding_input` is the new
`vize_impeto` file; `lattice.rs` only mounts it. `evaluate_binding` is the
classifier. The folio page is unchanged.

## Kind to lattice input

| kinds | effects | verdict | class |
| --- | --- | --- | --- |
| `ref`, `shallowRef`, `computed`, `toRef`, `toRefs` | `read-reactive` | proven | reactive |
| `reactive`, `shallowReactive` | `read-reactive`, `mutate-local` | proven | reactive |
| `readonly`, `shallowReadonly` | `read-reactive`, `freeze` | unknown | reactive |

`readonly` stays unknown because the wrapped value is not visible. Losses are
copied in tracker order and are not a second classification.

## Acceptance

- TS-34 lattice matrix: 4 origins × 256 effect subsets × 4 escapes × 3 verdicts = 12288 facts, zero divergences.
- TS-34 corpus (`ant-design-vue`, `create-vue`): artifacts=773 compared=361 facts=764 skipped=412 `no-reactive-source`.
- Battery, ladder, and the P2-15 matrix: zero reactivity divergences.
- `[s3-reactivity-folio]` canonical text and `formal/impeto/fixtures/reactivity-lattice.folio` byte-identical.
- `cargo test -p vize_croquis_cf --lib reactivity`: 37 passed. The CLI TS-9 fixture suite was not re-run; the group returns the same sources, losses, and kinds.
- `cargo test -p vize_davinci --test fact_demand`: 6 passed.
- Consumption `--check` up to date. `davinci-matrices.test.ts`: 10 pass.

## Readers left on the tracker

- `vize_atelier_core` `get_reactive_kind`: a per-identifier demand on the fused compile lane. DOM `with_reactive_reads` demands the group once per compile.
- Canon `virtual_ts` template refs: lane C.
- Vitrine `overlay()`: the reactivity-overlay product, not a lattice row.
- Patina reads `ScriptParseResult.reactivity`, the population tracker.
- Tests that `register` a source are writers.
