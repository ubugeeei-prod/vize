# Phase 1 — One Arena, Real Expressions

> [!NOTE]
> The highest-leverage phase: expressions parsed once, strings out of nodes,
> one lifetime. Exit requires a measurable performance **win**, not parity.
> Dependency chain is real here — order matters. In-phase fallback flags per
> charter #26; P1-13's gate records what each of them turned out to be.

## Phase 1 outcome

**Executed 2026-08-14 → 2026-08-15. Eleven of thirteen tasks landed at the
original exit; P1-8 has since been resolved by the scanner deletion follow-up,
P1-12 awaits maintainer sign-off, and the exit gate below still carries the
remaining named blockers.** The phase did not exit clean, and per charter #26 the surviving
old paths are not "flag fallbacks to switch off" — they are unfinished
deletions with owners and issues, enumerated in the gate.

**What landed.** One `Allocator` per compile (P1-1), threaded through every
template lane (P1-2). `SourceLocation` went from `{ source: String, start:
Position, end: Position }` to `{ span: Span }` — 48 B → 8 B — by retiring
per-node source strings (P1-3) and then line/column (P1-4, where the parser's
tracking turned out to be degenerate by construction). Template expressions
are parsed **once**, at template parse, into that arena and retained on the
node (`SimpleExpressionNode::js_ast`, P1-5); croquis (P1-6) and the atelier's
transform rewrite, codegen prefixer and vapor generate (P1-7) read the
retained AST instead of re-parsing, and identifier prefixing became an AST
transform emitting through a span splicer (P1-9). Node strings moved into the
arena, the manual `Drop` impls were deleted and the two-pool handle collapsed
onto `oxc_allocator` (P1-10). One arena per rayon worker, reset between files
(P1-11). The performance page and its four locale mirrors were rewritten
against the shipped compiler (P1-12).

**Headline measured numbers** (conditions in each task's record):

- **Legacy expression re-parses per fused compile → floor** (P1-7, pinned in
  CI): ssr `stress-interp` **500 → 0**; dom `large` 106 → 16, ssr `large`
  155 → 27, vapor `large` 45 → 8; every other ladder cell **0**.
- **Corpus batch peak RSS** (P1-11, `vize build` over 36 541 corpus files):
  766.5 MB → **171.1 MB** (−77.7%, 12 workers); 717.0 MB → **88.2 MB**
  (−87.7%, one worker). Unreleased allocations over a 731-file project
  12 448 → 4 932 (−60.4%).
- **Allocations per run** (deterministic, machine-independent; the new
  ratchet, now recorded in `budgets.toml`): `armature_parse` phase-start →
  phase-end `stress-interp` 620 → **105** (−83.1%), `stress-deep` 517 →
  **155** (−70.0%), `large` 479 → **272** (−43.2%). Fused compiles at P1-10
  alone: dom `stress-interp` 1048 → 536, ssr `stress-interp` 3149 → 2637,
  vapor `stress-interp` 1495 → 974.
- **Node sizes** (P1-10 const asserts): `RootNode` 336 → 224, `ElementNode`
  208 → 104, `SimpleExpressionNode` 144 → 88, `AttributeNode` 192 → 56,
  `TextNode` 72 → 24, `SourceLocation` 48 → 8.
- **Corpus parity**: byte-identical throughout — 568 rows × 142 projects,
  zero gating drift at the phase-final tree.

**Explicit pendings** (all carried into the gate below): the legacy re-parse
chain in `reparse.rs` (P1-9's 11.73% non-admitted inputs), the codegen-side string-rewrite
emission, the never-pinned bench improvement target, an alloc ratchet that is
recorded but not yet **enforced** by `bench-compare.mjs`, corpus peak RSS with
no field to live in until P5-11, and the `davinci-differential` verification
lanes, whose written life is "for one release".

## TODO index

- [x] P1-1 Arena unification in carton
- [x] P1-2 Allocator plumbing through armature/atelier
- [x] P1-3 `SourceLocation` diet: retire per-node `source` strings
- [x] P1-4 `SourceLocation` diet: retire dead line/column
- [x] P1-5 Retained expressions: parse-once storage
- [x] P1-6 Consumer migration wave A (croquis identifier/scope helpers)
- [x] P1-7 Consumer migration wave B (atelier patch-flag, v-for, transforms)
- [x] P1-8 Delete the fast/slow scanner split — resolved by taking `scanner-parity-report.md` option 2: fix the output-visible walk/scanner bugs first, then delete the byte scanner with only the audited raw-surface differences admitted
- [x] P1-9 Identifier prefixing as AST transform
- [x] P1-10 Node strings → `&'a str` / arena atoms; delete manual `Drop`s
- [x] P1-11 Arena reuse across files (batch pool) — corpus batch peak RSS 766.5 MB → 171.1 MB (−77.7%, 12 workers) / 717.0 MB → 88.2 MB (−87.7%, one worker)
- [ ] P1-12 Performance-doc truth pass — page + four locale mirrors rewritten with the claim inventory committed below; **maintainer sign-off pending** (its acceptance is a review point)
- [ ] P1-13 Phase exit: budgets pinned, old paths deleted — gate evaluated and recorded below: alloc ratchet pinned and the stale-comment sweep done, but **old paths are not fully deleted** (P1-9 residual) and two gate lines have no target to measure against

---

## P1-1 — Arena unification in carton

**Steps:**

- [x] `crates/vize_carton/src/allocator.rs`: `Allocator` becomes the unified per-compile handle `{ bump: Bump, oxc: oxc_allocator::Allocator }` — one value, one lifetime, one `reset()` covering both pools; `as_oxc()` exposes the retained-AST pool P1-5 parses into _(amended in-PR: the original step assumed oxc_allocator was still bumpalo-backed and the container aliases could flip here. At the pinned rev (0.142, `fc702c1`) oxc ships its own `Arena` — no bumpalo inside — and `oxc_allocator::{Box, Vec}` reject `Drop` payloads with a const assertion, which today's string-owning nodes trip. The aliases flip at P1-10 once the string diet makes nodes `Drop`-free; until then the two-pool handle is the chartered in-phase transitional state (#26))_
- [x] Audit bumpalo-API deltas used in-tree _(finding: `Deref<Target = Bump>` is preserved, so every `Box::new_in`/`Vec::new_in`/`BumpString`/`BumpVec` call site compiles unchanged; `alloc_str`/`as_bump`/`reset` keep their signatures; `allocated_bytes` now reports both pools' bytes — sole external consumer is vize_maestro's virtual-code metrics, where the sum is the truthful number; nothing needed a shim)_
- [x] Node-size static asserts from P0-9 unchanged _(no node type changes in this task)_

**Acceptance:** workspace compiles, full test suite green, P0 benches within noise, `tools/commands/davinci/corpus-diff.rs` empty.
**Deps:** P0-4, P0-9.

## P1-2 — Allocator plumbing

**Steps:**

- [x] Thread `&'a Allocator` through `vize_armature` entry points (`crates/vize_armature/src/parser/entry.rs`, `parser.rs`) and the atelier lanes (`vize_atelier_core::lane::transform`, sfc `compile_template`) so template structures and future oxc ASTs share `'a` _(landed: the six `entry.rs` parse wrappers take `&'a Allocator` and extract `allocator.as_bump()` at the boundary — `Parser::*` internals still see `&'a Bump`; `TransformContext.allocator`, its constructors, the four `lane.rs` transform functions, and the `lane/extensions.rs` wrappers carry the handle. The plumbing necessarily extends through the dom/ssr/vapor lane compile entries — they sit between sfc and the lane and a `&Bump` cannot produce a handle — and `compile_template_block` plus its vapor sibling lose their local `Bump::new()`: the birth site is one `vize_carton::Allocator::new()` per template block compile in `compile.rs`/`template_only.rs` (batch pooling stays P1-11). Deref coercion keeps every `Box::new_in`/`Vec::new_in`/codegen call site unchanged)_
- [x] `vize_atelier_jsx` (already oxc-based) switches its local allocator to the caller-provided one _(landed: `lower_source`/`lower_source_with_compat` gain a caller-provided `&oxc_allocator::Allocator` and the local `Allocator::default()` is deleted; the full-compile entries (`compile_jsx*`, `compile_to_vdom/ssr/vapor`) take `&vize_carton::Allocator` because they run the shared transform lane, and split it via `as_bump()`/`as_oxc()` at the lower boundary. Analysis-only callers (maestro, patina, canon) hold a carton handle and pass `handle.as_oxc()`; the babel pragma-probe's throwaway parse keeps its local oxc arena. Verified: workspace + all-targets + napi/wasm/legacy feature builds compile, both clippy gates clean, `VIZE_TEST_REQUIRE_TSGO=1 cargo test --workspace` 321 suites 0 failed; corpus-diff runs with the orchestrator's rebase, per protocol)_

**Acceptance:** corpus-diff empty; benches hold; no public-API regressions the CLI/vitrine can't absorb (charter #23: internal breakage is free).
**Deps:** P1-1.

## P1-3 — Retire per-node `source` strings

**Steps:**

- [x] `SourceLocation` (in `crates/vize_relief/src/relief/core.rs`): remove `source: String`; add `span: Span` (P0-9 type); every consumer from `davinci-road/plan/sourcelocation-inventory.md` switches to `Span::slice(source_text)` — the inventory, a generated artifact, regenerates to a zero-`source`-read scan as the migration record, and its generator now fails regeneration if a `.source` read comes back _(amended in-task: the original "inventory rows checked off in the PR" wording predates the file being generator-owned; the regenerated scan plus the generator ratchet is the checked-off form. Landed: `SourceLocation` is `{ start, end, span }` with the span derived from the offsets in `new`, kept in sync by `set_end` on the parser's text-merge path, and const-asserted at 32 bytes. The slicing basis is the exact string the parser parsed: `RootNode::source` in the template lane, threaded as `TransformContext::source`, a new `CodegenContext::source`, `SsrCodegenContext::source`, the vapor transform/generate contexts (`RootIRNode::source` now carries the real text instead of `""`), croquis `Drawer::template_source` / `VirtualTsGenerator::template_source`, patina's lint contexts and query-collector sinks, and canon's slot-outlet collection. JSX roots keep the root element's slice in `RootNode::source` for the source-map path while node spans index the whole module, so the JSX lanes pass the module text explicitly — `lane::transform_with_source_text`, a `source_text` override on the vdom codegen entry, and source params through the jsx ssr/vapor compiles)_
- [x] Diagnostic excerpt rendering reads via span + the file's source text (threaded where missing) _(landed: the only output paths that embedded a location's covered text were the debug-formatted error lists in the SFC template gate (`compile_template.rs`) and the vitrine binding parse-error strings; they now render through `CompilerErrorWithSource`, which prints byte-for-byte what the pre-span derived `Debug` printed — `source` field included, sliced from the span — pinned by an exact-equality test in `vize_relief`)_
- [x] Re-pin node-size static asserts (they shrink) _(P0-9 placed only the `Span == 8` assert, which is unchanged; the shrunk `SourceLocation` is now pinned at 32 bytes (was 48). Measured node sizes: ElementNode 208→176, AttributeNode 192→144, DirectiveNode 240→224, SimpleExpressionNode 144→128, CompoundExpressionNode 120→104, TextNode 72→56, InterpolationNode 64→48, CommentNode 80→64, ForNode 216→200, IfNode 80→64, RootNode 336→320, CompilerError 80→64)_

**Acceptance:** corpus byte parity **including diagnostic text**; alloc counts drop in P0-2 benches (record the delta); inventory file updated to all-migrated. _(P0-2 alloc deltas (`cargo bench --bench davinci -- --quick`, before → after): armature_parse large 479 → 300 (−179), stress-deep 517 → 395 (−122), stress-wide 321 → 220 (−101), medium 236 → 171 (−65), small 25 → 22 (−3), stress-interp 620 → 620 (±0 — that fixture's loc strings all fit CompactString's inline capacity, so they never heap-allocated); peak transient heap drops with them (large −11.3%, stress-wide −6.0%, stress-deep −5.6%). Tokenize lanes unchanged, as expected. croquis_analyze_* shows +1 alloc per run: the drawer now captures `RootNode::source` once as its slicing basis (`Drawer::template_source`), one template-sized copy that also appears as the peak-heap bump on the stress fixtures (stress-deep 6.4 KB → 16.2 KB) — the per-node string savings land on the parse side that feeds it.)_
**SemVer gate:** the removed `SourceLocation::source` and the threaded-basis signatures are public API on four published crates (`vize_relief`, `vize_atelier_core`, `vize_atelier_ssr`, `vize_atelier_vapor`), so the change carries a conventional breaking marker (`feat(davinci)!:` plus a `BREAKING CHANGE:` footer) — the declaration `docs/content/stability.md` requires of `cargo-semver-checks`. Charter #23 governs the waiver: Vize-internal crate APIs break freely until contracts GA in phase 6, while emitted output holds the hard byte-parity bar.
**Deps:** P1-2, P0-9.

## P1-4 — Retire dead line/column

**Steps:**

- [x] `Position` reduced to `offset: u32` (or `Position` deleted in favor of `Span`); line/col derived only in diagnostic rendering and `SourceMapBuilder::finish()` (`crates/vize_atelier_core/src/codegen/source_map.rs` — it already re-derives from offsets because parser line info was unreliable) _(landed as the **delete** option: with line/column gone an offset-only `Position` would have duplicated the span endpoints behind a sync invariant, so `SourceLocation` is now the 8-byte `{ span: Span }` (const-asserted; was 32) and every `loc.start.offset` / `loc.end.offset` read moved to `loc.span.start` / `loc.span.end` while P1-3's `loc.span.slice(...)` sites compiled unchanged. One byte-parity finding: the parser's `Position` values were degenerate by construction — `Parser::get_pos` binary-searched a newline table **nothing ever populated**, so every stored value was `line: 1, column: offset + 1` — and the sole output path that printed them (`CompilerErrorWithSource`, the SFC gate / binding-boundary debug rendering) has those frozen bytes pinned by check oracles (`tests/snapshots/check/vue-router-patch-oracle.ts` pins `offset: 157, line: 1, column: 158`). The renderer therefore reproduces the frozen `line: 1, column: offset + 1` shape from the span offset instead of deriving real values via `line_index`; flipping that output to true derivation is a recorded, corpus-visible behavior change for a future task to take through the corpus gate deliberately. Real-derivation consumers (source maps, patina, LSP collectors) were already offset-based and are untouched. Residual divergence window, unreachable in the test suite: v-for source sub-locations previously advanced line/column per-char over the alias prefix (`advance_position`, now pure offset arithmetic), so a **fatal** error on a v-for source expression whose alias prefix contains a newline or non-ASCII would have printed the advanced values where the frozen shape now prints — no test or oracle covers that combination, and offsets (what lint/LSP consume) are byte-identical throughout. Maestro's dead `source_location_to_range` / `internal_to_lsp_position` converters (0 callers) are deleted; jsx `SpanMapper` no longer builds a per-module `LineIndex` to eagerly expand positions. The sourcelocation-inventory generator now ratchets all five member paths and asserts the deleted carriers stay gone.)_
- [x] Delete the parser's vestigial line-tracking fields _(deleted: `Parser.newlines` (allocated but never pushed — the degeneracy above), `Parser::get_pos`, `Tokenizer.newlines` + `Tokenizer::get_pos` (production-dead: only its own tests called it) and the per-byte newline pushes in `Tokenizer::tokenize` / `fast_forward_to`; the text-merge path now calls `loc.set_end(offset)` directly. Node sizes (P1-3 → P1-4 bytes): SourceLocation 32→8, ElementNode 176→128, AttributeNode 144→72, DirectiveNode 224→200, SimpleExpressionNode 128→104, CompoundExpressionNode 104→80, TextNode 56→32, InterpolationNode 48→24, CommentNode 64→40, ForNode 200→176, IfNode 64→40, RootNode 320→296, CompilerError 64→40.)_

**Acceptance:** corpus parity including source maps; node sizes re-pinned smaller.
**Deps:** P1-3.

## P1-5 — Retained expressions: parse-once storage

**Steps:**

- [x] `crates/vize_relief/src/relief/expressions.rs`: `JsExpression<'a>` replaces the `raw: String + PhantomData` stub with `ast: &'a oxc_ast::ast::Expression<'a>` (+ `raw: &'a str` slice for display); decide parse point by bench — during template parse (armature) vs first semantic touch (croquis) — record the measurement in the PR _(landed as `{ ast: &'a Expression<'a>, raw: &'a str }` carried in the existing `SimpleExpressionNode::js_ast: Option<JsExpression>` — the `Option` is the honest boundary: `Some` iff the content parses as **one complete** TS-dialect expression covering the whole text (`SourceType::ts()`, matching the croquis consumers' `expr.ts`; completeness = whitespace-only remainder after `ast.span().end`, required because oxc `parse_expression` does not demand end-of-input and would silently return `a++` for `a++; b++`). v-for values (`item of items`), v-on statement bodies, and invalid text stay `None`; P1-6/7 consumers keep their own handling for those shapes. `raw` is the template-source slice when the accumulated content equals it (interpolations always; values without entities), else an oxc-arena copy of the decoded content (entity-decoded values, camelized same-name shorthand). **Parse point: template parse** — armature's four non-static build sites (directive value, dynamic argument, shorthand value, interpolation) call one `retain_expression_ast` helper (`crates/vize_armature/src/parser/expression.rs`); `Parser` constructors take `&'a vize_carton::Allocator` and split bump/oxc at the boundary. **One differential finding**: the unguarded retained parse crashed the #956 stack-overflow reproducer (`vize_atelier_dom`'s `expression_nesting_guard` suite — 2 704 nested `[`) because oxc's recursive parser cannot be depth-limited; the expression nesting guard therefore moved from the historical `vize_atelier_core::transforms::transform_expression::nesting` path (now `vize_atelier_core::steps::expression::nesting`) to `vize_carton::expression_guard` (verbatim, import paths preserved by a re-export shim; six `scan` helpers widened `pub(crate)`→`pub` for the cross-crate comment-rewrite consumer) and the retained site refuses unsafe text (`expression_is_safe_to_parse`) **before** parsing and before counting — the same refusal every legacy site applies before creating its parse arena, so guard-refused expressions are parsed zero times everywhere and the counter law's "distinct expressions" means guard-accepted ones. Measurements, `cargo bench --bench davinci -- --quick`, before → after wall p50 (final tree, guard included): armature_parse small 1.53→1.76µs (+15.5%), medium 13.16→15.50µs (+17.8%), large 46.15→55.12µs (+19.4%), stress-deep 41.85→44.96µs (+7.4%), stress-wide 75.37→82.38µs (+9.3%), stress-interp 67.45→106.22µs (**+57.5%**, ≈78ns per expression incl. the guard scan; the pre-guard intermediate run measured +51.6%/≈70ns, so the guard's own share is small); allocs +1 per guarded expression with delimiters (the guard's scratch stacks, e.g. large 293→350) plus the oxc pool chunk in peak (+16–50 KB). Fused compiles pay the same absolute cost (dom stress-interp 159.6→196.6µs +23.2%, ssr large +22.1%, vapor large +17.3%, most others +2–12%; single `--quick` samples carry outliers — dom_compile_large printed +113.5%, inconsistent with its own +19.4% parse stage on a ~30% parse share, and the parse-free `atelier_ssr_codegen_*` stage drifted +22–25% on identical inputs in the same runs — the parse-stage numbers are the clean signal); croquis analyze windows exactly ±0.0% (parse outside the window). The P0-3 legacy probe counts are byte-identical to `expr-reparse-baseline.md` before and after (all 18 backend×fixture lines). The alternative, **parse-on-first-touch (lazy `OnceCell` per node + `retained_ast(&'a oxc Allocator)` accessor), was implemented and did not reach a benchable state**: any interior mutability over `'a` makes every template node invariant over the arena lifetime, and the tree's walk/visitor architecture is built on covariant shortening — 13 helper signatures de-unified mechanically (croquis virtual_ts ×2, atelier_core codegen ×4, atelier_ssr ×6 incl. arena-allocating `SsrCodegenContext`'s options, atelier_jsx ×3) still left 37 errors in vize_patina (incl. the public `Copy` rule-IR type `MarkupDocument<'a>` whose unified `&'a RootNode<'a>` underlies every markup rule) plus 43 more in maestro+canon and the jsx/patina test helpers: a workspace-wide variance refactor that would also tax every future consumer, permanently. Chosen by those numbers: template parse — its ~70ns/expression cost is transient for consumed expressions (P1-6/7 delete the legacy re-parses, today 0–500 per fixture/backend on top of this), while first-touch's cost is structural and forever. The template-parse regression above means the "benches hold or improve" clause is **not** met inside P1-5 alone; the phase design books the payback into P1-6/7 (their acceptance asserts the `davinci.expr.parses` drop and croquis bench improvement) — chartered transitional state (#26))_
- [x] Parse via `oxc_parser` with the shared arena from P1-2; template-expression parse errors keep today's diagnostic behavior (differential-checked) _(the single site parses into `allocator.as_oxc()` and **swallows failures** (stores `None`): today every template-expression diagnostic is produced by the legacy on-demand re-parse sites, which this task leaves running unchanged (`expr_parse_probe` intact, its per-fixture counts byte-identical to `expr-reparse-baseline.md` before and after), so diagnostic bytes are unchanged by construction — verified by the full suite + insta corpus passing untouched)_
- [x] Profiler counter `davinci.expr.parses` incremented at the single parse site; exported via P0-11 _(one `global_profiler().record_counter("davinci.expr.parses", 1)` per attempt inside `parse_retained` — an attempt is a parse, so the counter equals non-static expression nodes built by the parser whether or not oxc accepts the text; flows through the standard counter export (`--profile-json`). Law pinned by `crates/vize_armature/tests/davinci_expr_parses.rs`: a 9-distinct-expression template yields samples == total == 9 and `Some`/`None` exactly per the completeness contract)_
- [x] Lifetime note enforced: retained `&'a Expression` values are per-compile ephemera under the architecture's arena/cache contract — anything crossing a compile boundary (caches, folios, summaries) uses the owned serialized form, never the arena reference _(audited: `CroquisFolio` (vize_davinci) and `Croquis` (vize_croquis) carry no lifetime parameters — fully owned, so the compiler forbids storing the ref; maestro's resident virtual-doc ASTs keep the whole two-pool carton handle alive alongside the tree, so oxc-pool refs share exactly the bump refs' validity; relief nodes are not serde-serializable, so no serialization path can capture it. Node sizes: all unchanged (`JsExpression` 24→24 — `&ast` + `&str` replace the 24-byte `CompactString` stub with the `Option` niche preserved via the non-null ref; `SimpleExpressionNode` 104, `ElementNode` 128, `DirectiveNode` 200, `RootNode` 296 as at P1-4); the only node-size const assert (`SourceLocation == 8`) is untouched)_

**Acceptance:** the counter law matches the chosen policy — **each expression
parsed at most once (zero re-parses) always**; under parse-at-template-parse,
counter == distinct expressions; under parse-on-first-touch, counter ≤
distinct expressions and untouched expressions are provably never parsed.
Corpus parity; benches hold or improve.
**Deps:** P1-2.

## P1-6 — Consumer migration wave A (croquis)

**Steps:**

- [x] `crates/vize_croquis/src/drawer/helpers/identifiers/ast.rs` — reads the retained AST instead of re-parsing where the node carries `js_ast` and `strip_js_comments` left the text unchanged; fallback classes without retained AST or with rewritten comments parse through the same AST implementation. The P1-8 follow-up later deleted the former byte scanner, renamed this module from `slow.rs` to `ast.rs`, and kept the retained-vs-reparse differential assertion for the comment-free path.
- [x] `crates/vize_croquis/src/drawer/helpers/v_for/oxc.rs`, `visit_element/second_pass.rs` — same _(second_pass landed: `<component :is>` reference recognition (`component_reference_expression`) shape-checks the retained AST directly and the legacy throwaway parse dies for `Some` nodes; `None`/compound keep it. v_for/oxc.rs is a **recorded no-fork**: its parses are binding-pattern parses over synthesized `let [<alias>] = x` text — text that never existed as a template expression, so no retained AST corresponds to it — and the v-for value's own retained AST must not be consumed either (JS `in` associates left while Vue's v-for grammar splits at the first viable `in`/`of` boundary; they disagree on `a in b in c`). The drawer also holds no per-compile allocator handle (`Croquis`/`Drawer` are owned and lifetime-free under the arena/cache contract), so per the fallback clause the local arenas stay for exactly these binding-pattern parses, recorded in the module doc; they unify at P1-8)_
- [x] Differential lane: for one release, CI compares old-path vs new-path results (identifier sets, v-for destructure shapes) over the corpus; divergences are bugs in one side — investigate, don't average _(landed as `#[cfg(any(test, feature = "davinci-differential"))]` dual-run comparators **inside the migrated helpers** — every retained identifier walk and component-reference check re-runs the legacy re-parse and `assert_eq!`s (divergence panics; nothing averaged) — plus process-global counters in `vize_croquis::drawer::differential`; zero-cost when off (module and call sites compile out). The plain suite arms them via `cfg(test)` (`drawer/tests/differential.rs` pins a coverage floor so a cfg regression that disarms the lane fails loudly). Corpus-runnable entry, exact command: `VIZE_DAVINCI_DIFFERENTIAL_CORPUS=tests/_fixtures/_git cargo test -p vize_croquis --features davinci-differential --test davinci_differential -- --nocapture` (the committed battery + P0-2 ladder run with exact-pinned counts — 8/3/2 battery, 24/5/4 with ladder, the pin doubling as a dispatch/retention ratchet — and the env var widens the sweep to every `.vue` under the dir). Measured 2026-08-15 over the hydrated corpus: 41 580 files analyzed, **37 260 identifier comparisons and 1 649 component-reference comparisons, zero divergence**; 13 235 v-for shapes witnessed (a pre-gate sweep that also dual-ran comment-carrying text measured 37 540 identifier comparisons, likewise zero divergence — the comment-free gate is a proof-shape decision, not a reaction to a found divergence). The v-for lane is a witness count, not a dual-run: wave A forks no v-for path (previous step), so old and new are the same code and the fork-level comparison becomes real with P1-7/P1-8; v-for shape parity over the corpus is carried by the standard output-level corpus gate meanwhile)_
- [x] Charter #37 note: only the _input layer_ moves; tracker internals untouched _(holds: the diff touches the identifier/component-reference input helpers, the `ids.rs` node plumbing, the v_for no-fork record, and the cfg-gated lane; `scope.rs`, the trackers, and every croquis product type are untouched — `rust-script tools/commands/davinci/croquis-consumers.rs --check` reports the consumption matrix unchanged)_

**Acceptance:** differential lane green over the corpus; `davinci.expr.parses` drops (record); croquis bench improves. _(Differential: green, counts above. Counter record — the plan line oversimplified: `davinci.expr.parses` counts armature's retained parses and is **unchanged by construction** (wave A deletes croquis-side re-parses, which were never in that counter, nor in `expr-reparse-baseline.md` — that baseline instruments atelier only and its 18 backend×fixture lines were re-verified byte-identical before and after this task from the bench stderr). What drops is croquis's uncounted analysis-side re-parse total: each identifier/component-reference comparison above is exactly one avoided `Allocator::default()` + oxc parse in production — 38 909 avoided parses per corpus analysis sweep, 18 per full+for_compile ladder sweep. Bench record (`cargo bench --bench davinci -- --quick`, P1-5 tree → this tree): croquis_analyze_full_large p50 177.4→165.6µs (**−6.7%**; a pre-gate pass read −11.6% — single `--quick` samples), allocs 615→592 (**−23**, deterministic in every run); for_compile_large allocs 297→296 (the `:is` parse; its +3.4% wall print is single-sample noise against the alloc drop); remaining croquis lanes −6.8…+2.0% wall with allocs unchanged (few complex expressions). Armature and fused lanes: alloc counts byte-identical on all 66 lanes; wall within `--quick` noise (armature −4.3…+2.5%; dom's first after-pass printed +49…+107% outliers on alloc-identical lanes and a rerun read −10.2…+3.4% — the same single-sample outlier mode the P1-5 record documents; ssr −3.1…+3.9%; vapor −7.8…+11.4% on sub-µs lanes). The P1-5 parse-side regression itself is untouched here — its remaining payback is the atelier re-parse deletion, booked at P1-7 as the phase design states)_
**Deps:** P1-5.

## P1-7 — Consumer migration wave B (atelier)

**Steps:**

- [x] `crates/vize_atelier_core/src/codegen/patch_flag.rs` — retained AST, local arena dies _(landed: the static-literal classification moved to `codegen/patch_flag/static_literal.rs` with a node-aware entry — for gated nodes the retained AST is shape-walked directly and the local arena dies; the legacy trimmed-wrapped parse stays as the fallback body. The gate used at every wave-B site is two-part and self-validating: `retained::retained_whole_expression` (the node's `js_ast.raw` must equal its current `content` byte-for-byte, so any post-parse rewrite disarms the read) and `retained::js_module_compatible` (the retained parse is TS/unambiguous while the legacy sites parse JS/module — the scan admits an AST only when no construct could parse differently between the dialects: TS-flavored nodes or fields, strict/module-reserved identifiers, sloppy-only literal spellings (`010`, `"\05"`), `<!--`/`-->` lexer markers, `with`, `delete <ident>`, and whole function/class expressions; everything rejected keeps the legacy parse). For an admitted AST the legacy parse is the same parse — byte equivalence is parser determinism, the P1-6 proof shape)_
- [x] Remaining reparse sites from the P0-3 counter baseline (~20 in `vize_atelier_core`), checklist generated from that baseline and checked off per site _(the probe instruments 18 sites — 16 in `vize_atelier_core`, 2 in `vize_atelier_vapor` — enumerated from the `parse_arena()` insertions; per-site record: **[1] codegen/patch_flag.rs:84 → migrated** (above); **[2] codegen/helpers.rs is_constant_simple_expression → migrated** (split to `codegen/helpers/constant_expression.rs`; span-free visitor walks the retained AST); **[3] codegen/expression prefix_identifiers_with_context expr parse → migrated** (split to `expression/prefix_context.rs` + `prefix_visitor.rs`; node entry walks retained spans at offset 0 vs the wrapper's offset 1; consumers: `generate_event_handler` when the processed text is still the node's bytes, and dynamic-arg template-literal keys in props/directives); **[4] its program-parse branch → kept**: multi-statement handlers have no whole-expression retained AST (P1-5 completeness contract) and the branch is reachable only after the expr parse failed; **[5] codegen/slots/params.rs prefix_slot_defaults → kept**: synthesized `(props) => null` arrow over slot binding patterns; **[6] steps/v_model.rs legacy-conditional probe → kept**: quirks-gated compat path whose value arrives as a detached string (possibly a compound slice), JS dialect when `!is_ts`, zero ladder occurrences; **[7]+[8] is_event_handler_reference_expression / is_function_expression → migrated at consumers** (node entries in `steps/expression/shape_checks.rs` used by inline_handler, dom/ssr event codegen, and SSR component props when the checked text equals the node's bytes; the string entries stay for genuinely transformed text); **[9] steps/v_slot/params.rs extract_slot_prop_names → kept**: synthesized `let <pattern> = __slotProps` binding-pattern parse (the slot analogue of P1-6's v_for no-fork); **[10] prefix.rs prefix_identifiers_in_expression → kept**: pub string API with no node-holding production caller (nesting-guard suites exercise it); collapses at P1-9; **[11] typescript.rs strip_typescript_from_expression → kept**: mutating oxc semantic+transformer pass over a synthesized `const _expr_ = (content);` program — the retained AST is shared and immutable; **[12]+[13] rewrite.rs parses_as_typescript → short-circuited** (moved to `steps/expression/parse_checks.rs`; a dialect-gated retained AST is itself proof the original parses as TS, so the diagnostic decision skips both parses; the parses remain for None-retained text); **[14] rewrite.rs parse_as_params → kept**: synthesized `(params) => null`; **[15] rewrite.rs expression parse → migrated** (`steps/expression/retained_rewrite.rs`: the IdentifierCollector walks the retained AST in content coordinates — `new_unwrapped`, which also reproduces the legacy wrapper's one observable artifact, the assignment-target paren-scan overshoot drop — this is the lane that owned SSR's 500 stress-interp re-parses); **[16] rewrite.rs program parse → kept**: multi-statement bodies; **[17] vapor generate/expression.rs expr parse → migrated** (`generate/expression_retained.rs` + `GenerateContext::resolve_expression_node`; every node-holding caller swapped — SetText/SetProp/SetHtml/SetEvent values and keys, if/for conditions, refs, slots, component props — with retained spans applied at the leading-whitespace offset since the resolver rewrites trimmed text; the vapor legacy dialect is TS/module so the shared gate is deliberately over-conservative there, TS-syntax expressions stay on the fallback parse); **[18] its program branch → kept**: multi-statement. Support work the checklist rests on: `JsExpression` derives `Copy` and `SimpleExpressionNode::from_node` clones a node with its retained parse; the clone/normalize paths in steps/expression.rs, lane/context.rs, lane/structural.rs (`SimpleExpressionContent` gained the lifetime + `js_ast`), and 32 vapor-transform IR constructions now propagate `js_ast` through byte-identical clones — content-rewriting paths still drop it, and the `raw == content` gate makes any missed propagation a safe fallback, never a wrong read)_
- [x] Same differential-lane pattern as P1-6 _(landed: `#[cfg(any(test, feature = "davinci-differential"))]` dual-run comparators inside every migrated read — the transform rewrite walk, the codegen prefixer walk, the vapor resolve walk, and each boolean shape check re-run the legacy parse in an **uncounted** arena (`oxc_allocator::Allocator::default()`, not the probe constructor, so lane-only work never disturbs the re-parse floor) and `assert_eq!` byte-for-byte; divergence panics, never averages. Process-global counters in `vize_atelier_core::retained::differential` (compiled unconditionally — a few dead atomics in production — so core's and vapor's independently-armed comparators share one ledger). Plain-suite arming via `cfg(test)` with coverage witnesses that fail loudly on a cfg regression (`vize_atelier_core` `retained/tests.rs`, vapor `tests_davinci_differential.rs`). Corpus-runnable entry, exact command: `VIZE_DAVINCI_DIFFERENTIAL_CORPUS=tests/_fixtures/_git cargo test -p vize_atelier_sfc --features davinci-differential --test davinci_differential -- --nocapture` — the committed battery + P0-2 ladder compile through the full SFC pipeline three times per source (dom/ssr/vapor) with exact-pinned comparison counts (battery 18/0/7/8, ladder 1408/0/64/252 across transform/codegen/vapor/shape lanes; the codegen-prefixer lane pins 0 on SFC routes — the SFC pipeline prefixes in the transform — and is witnessed by the core unit test instead), and the env var widens the sweep to every `.vue` under the dir on a wide-stack thread (one recorded exclusion: the vapor lane is skipped for `lang="pug"`/`"jade"` templates, whose HTML-parsed degenerate trees abort a pre-existing unguarded vapor recursion — reproduced on the untouched P1-6 base tree with `wave-ui`'s `form/examples.vue`, so it is not a P1-7 divergence; tracked as its own fix, and dom/ssr still dual-run those files). Measured 2026-08-15 over the hydrated corpus (release): **41 580 files compiled, 801 305 transform-rewrite + 1 codegen-rewrite + 167 578 vapor-resolve + 152 542 shape comparisons, zero divergence**)_

**Acceptance:** `davinci.expr.parses` reaches its floor (== distinct expressions) on the corpus — asserted in CI from the profiler export; parity holds. _(Record — the counter law reading: the P1-5 profiler counter `davinci.expr.parses` counts armature's retained parses and is unchanged by construction (pinned by `vize_armature/tests/davinci_expr_parses.rs`, still green); what P1-7 drives to its floor is the **legacy re-parse count** measured by the P0-3 probe around one fused compile per ladder fixture — the bench recorders print those probe deltas under the same `davinci.expr.parses` stderr label, so the baseline doc's lines are exactly the numbers that dropped. Old → new (dom/ssr/vapor per fixture): small 1/4/0 → 0/0/0, medium 33/37/4 → 0/0/0, large 106/155/45 → 16/27/8, stress-deep 24/48/16 → 0/0/0, stress-wide 102/100/0 → 0/0/0, stress-interp 0/**500**/0 → 0/0/0. The migrated sites are at zero everywhere; the surviving counts are exactly the kept-with-reason classes on `large.vue`: dom 16 = 8 slot-pattern parses + 8 slot-default arrows; ssr 27 = 8 slot-pattern + 8 param-arrow validations + 10 SSR rendered-handler shape checks (transformed text) + 1 v-for source sub-expression (no retained AST by the P1-5 contract — the v-for splitter synthesizes it); vapor 8 = slot-pattern parses. CI assertion: `tests/davinci_expr_reparse_floor.rs` in each backend crate pins the per-fixture floors with exact equality (single-`#[test]` binaries, comparators disarmed there, so the deltas are the production floor); the post-P1-7 table is recorded in `expr-reparse-baseline.md` per its regeneration convention. Bench payback (P0-3 benches, `cargo bench --bench davinci -- --quick`, all runs same machine/day; base = a clean P1-6 worktree re-measured idle after the first baseline run was found contaminated by concurrent work — its alloc counts matched the committed values exactly, wall numbers were re-taken): fused compiles base → P1-7, wall p50 / allocs — dom: small 7.00→6.54µs (−6.6%, −1), medium 52.1→44.9µs (−13.8%, −41), large 185.8→148.6µs (**−20.0%**, −197), stress-deep 124.4→118.9µs (−4.4%, −48), stress-wide 154.7→149.7µs (−3.2%, **−154**), stress-interp 209.2→192.3µs (−8.1%, ±0); ssr: small 7.25→6.43µs (−11.4%, −7), medium 90.7→85.0µs (−6.2%, −49), large 219.4→174.3µs (**−20.6%**, −231), stress-deep 93.7→78.3µs (−16.4%, −72), stress-wide 150.3→141.7µs (−5.7%, −150), stress-interp 373.4→299.8µs (**−19.7%**, **−1000** — the 500 deleted parses × their two arena allocs); vapor: medium 57.2→54.1µs (−5.4%, −8), large 169.5→160.2µs (−5.5%, −86), stress-wide −1.9%/±0, and the three alloc-identical vapor lanes printed +46.4%/+8.6%/+2.6% single-sample outliers that re-ran at 8.37µs/101.8µs/310.4µs (−4.1%/+3.1%/+5.4%) — the documented `--quick` outlier mode on lanes P1-7 does not touch (vapor small/stress-deep/stress-interp execute zero gated reads). Stage windows on the counted lanes dropped consistently (dom transform large −36.2%, dom codegen stress-wide −39.2%, ssr codegen large −20.8%). **The P1-5 stress-interp debt, measured against the actual pre-P1-5 tree** (P1-4 rev 67a9c2d50 benched in a clean worktree, same session): armature_parse stress-interp 69.4µs pre-P1-5 → 104.7µs now (+50.8% — the parse stage keeps P1-5's retained-parse cost by design; P1-7 consumes, it does not re-price the parse); **ssr fused stress-interp 346.8µs pre-P1-5 → 299.8µs now = net −13.6%** — the lane whose 500 re-parses this phase targeted is ahead of where it started, with the re-parse deletion (−73.6µs vs the P1-6 base) repaying P1-5's ~+35µs parse cost twice over; dom fused stress-interp 164.0→192.3µs (+17.2%) and vapor fused 245.8→302.3µs (+23.0%) still carry the parse cost — those lanes had zero legacy re-parses to delete (their interpolations were never re-parsed on dom/vapor), so their repayment lever is P1-8's scanner deletion and P1-10's string diet, not consumer migration. Honest net: one lane repaid outright, two lanes hold P1-5's transitional cost with the payback booked to the phase's remaining deletion tasks (charter #26))_
**Deps:** P1-5 (parallel with P1-6).

## P1-8 — Delete the fast/slow scanner split

**Steps:**

- [x] Pre-deletion differential run: byte-scanner (`identifiers/fast.rs`) vs retained-AST walk over the whole corpus — committed report proving agreement (or documenting scanner bugs the AST walk fixes; those are waiver-reviewed) _(executed: **divergent — deletion blocked at this task's decision gate.** Over 41,580 corpus files: 327,806 fast-dispatch identifier comparisons, 12,607 divergent (3.85%), in 8 classes — the big items are scanner keyword-literal emission (consumer-inert by audit), assignment-target loss in the walk (2,450 texts, output-visible via mark-used/undefined-refs/TS6133), multi-statement tail loss (oxc `parse_expression` takes only the first expression), and two live scanner bugs the walk would fix (spread-argument drops; numeric-literal alpha tails inventing identifiers like `e22`). Full evidence: `scanner-parity-report.md`, incl. the two waiver-resolution options and their blast radii. The 314,108 retained-vs-one-parse crosschecks agreed everywhere)_
- [x] Delete `identifiers/{fast,slow}.rs` and the dispatch; single AST-walk implementation remains — option 2 accepted after assignment-target, statement-body, spread-argument, and numeric-literal fixes landed on the shipping path; the remaining keyword/literal and order/multiplicity/offset differences are the audited raw-surface waiver
- [ ] Bench check: if the scanner was faster on `stress-*` fixtures, the walk must close the gap before deletion merges (measured, not assumed) — the deletion variant now exists; the phase-start target/reference-runner gap remains recorded in the exit gate, so this line stays unticked until a comparable bench record is attached

**Acceptance:** grep zero for the deleted modules; corpus parity; croquis benches hold or improve.
**Deps:** P1-6, P1-7.

## P1-9 — Identifier prefixing as AST transform

**Steps:**

- [x] Replace string rewriting in `crates/vize_atelier_core/src/steps/expression/{prefix,rewrite,nesting}.rs` with an AST-level transform over retained expressions (scope-aware via croquis bindings) and span-preserving emission _(landed: on admitted inputs the rewrite is now a pure AST transform — `rewrite_expression` checks admission **first** (P1-7's `retained_whole_expression` staleness gate at the caller + `js_module_compatible`; on TS lanes additionally a byte-identity TS strip, since the stripper's detection scan can false-positive on TS-free text and rewrite bytes through its codegen round-trip), walks the retained AST with the existing binding-metadata/croquis-aware `IdentifierCollector`, and emits through the new span splicer `steps/expression/splice.rs`: one forward pass over the original text splicing exactly the collected identifier-span insertions (prefix/suffix), every other byte verbatim — never an oxc_codegen re-print. The splicer reproduces the retired end-to-start `insert_str`/`replace_range` loops bit-for-bit — out-of-range overshoot drop (P1-7's wrapper-paren class), same-position displacement order, prefix-before-suffix — with the retired loop kept verbatim as the unit-test oracle in `splice.rs` and as the differential comparator's legacy side. Admission also deletes the nesting-guard re-scans on that path: a retained AST is proof `expression_is_safe_to_parse` passed at the armature parse over the same bytes (`nesting.rs` stays the shared re-export shim; its scans now run only on the legacy chain). The legacy re-parse chain (wrapped `(content)` parse → program parse → simple-identifier fallback) moved verbatim to `reparse.rs`, emission-through-splice aside; its deletion is **not** this task — residual classes measured on the corpus (armed lane, per `rewrite_expression` call): **admitted 801,305 (88.27%) vs legacy 106,532** = 16,123 params-position (synthesized param validation, retained never applies), 82,645 unretained (no whole-expression retained AST for the bytes: compound-flattened expressions rebuilt from source slices, multi-statement handlers, v-for/slot synthesized sub-expressions, post-rewrite text — the P1-5 completeness contract), 6,874 dialect-rejected (`js_module_compatible` fallback classes), 890 TS-strip-rewrote (stripper false-positives that changed bytes); counters live in `retained::differential` (P1-9 additions), pinned exactly for battery+ladder in the sfc lane. `prefix.rs`'s standalone `_ctx.` prefixer keeps its own collector but emits through the same splicer; the props-alias projection (`rewrite_props_aliases`) deliberately stays a shared string post-pass on both paths — it is alias projection, not identifier prefixing, and runs byte-identically after either emission)_
- [x] Source-map assertions for rewritten identifiers added to the P0 fixture set _(landed: `crates/vize_atelier_core/tests/davinci_sourcemap_prefixed_identifiers.rs` — seven exact mapping-tuple assertions across five ladder fixtures (small `{{ label }}`/`{{ doubled }}`, large `:chartElement` bound prop, stress-deep `v-if="level8 > 0"` + `:class="cls4"`, stress-wide `:bound-25`, stress-interp `{{ item123 }}`): the single segment anchored at the generated `_ctx.<name>` position must equal the identifier's template (line, column) exactly, with every needle's uniqueness asserted so anchors cannot drift. `medium`'s only rewritten identifiers are event-handler values, which `generate_event_handler` emits without a segment (the #1533 fidelity scope) — pinned as such (4 rewritten, 0 anchored) rather than skipped silently)_
- [x] **Waiver budget: zero.** Prefixing output is the most visible codegen surface; any corpus diff is a bug in this task _(held: the armed comparator inside `rewrite_retained` dual-runs every admitted rewrite against the full legacy pipeline — wrapped re-parse **and** the retired string rewriter — and panics on any byte or helper-usage difference. Corpus run 2026-08-15 (release, hydrated `tests/_fixtures/_git`): **41,580 files × dom/ssr/vapor, 801,305 transform-rewrite comparisons, zero divergence**; the comparison counts match P1-7's recorded run exactly (801,305/1/167,578/152,542), so the admission set is provably unchanged while the mechanism underneath moved to the splicer. Battery/ladder pins in `vize_atelier_sfc/tests/davinci_differential.rs` unchanged (18 / 1,408) with the new legacy-class pins alongside (battery 0/4/2/0, ladder 16/12/2/0 params/unretained/dialect/ts-strip)_
- [x] Bench re-run (P0-3 recorders, `cargo bench --bench davinci -- --quick`, base = the P1-8 tree re-measured same machine/session, idle): the admitted path drops the guard re-scans on top of P1-7's re-parse deletion, and the splicer's exact-capacity single allocation replaces the retired loop's copy-then-regrow. Transform windows (base → P1-9, wall p50 / allocs): core stress-interp 15.00→13.66µs (**−8.9%**), stress-deep 7.19→6.74µs (−6.4%), large −1.0%, small −0.4%, medium +1.5%, stress-wide +0.5% — allocs ±0 on all six; fused ssr compile: stress-interp 304.3→278.1µs (**−8.6%**), large 165.7→159.8µs (−3.5%, **allocs −147**), medium −2.4% (−16), stress-deep (−16); dom/vapor fused lanes within noise with allocs ±0 everywhere. `--quick` single-sample outliers re-ran and settled (dom compile large +19.3%→+2.4%, ssr codegen small +25.6%→+1.3%, the vapor transform trio ≤+0.7%); `atelier_ssr_codegen_stress-wide` alone kept printing elevated singles (+23%/+32%) while its fused compile lane held −0.8% and allocs ±0 — an untouched path (SSR codegen prefixes through the codegen-side prefixer, not the transform rewrite), the documented outlier mode. Both JSON sets + logs saved under the task scratchpad (`p1-9-bench/{base,p1-9}`)

**Acceptance:** corpus byte parity on dom/vapor/ssr; new source-map assertions green. _(Met: zero-divergence dual-run above is the byte-parity witness on all three lanes; the seven mapping assertions plus the medium gap pin are green in `cargo test -p vize_atelier_core --test davinci_sourcemap_prefixed_identifiers`.)_
**Deps:** P1-7.

## P1-10 — Node strings to `&'a str` / atoms; delete manual `Drop`s

**Steps:**

- [x] Interner in `vize_carton` (arena-backed atoms for names appearing repeatedly: tags, directive names, well-known attrs); node fields (`name`, `tag`, `content`) become `&'a str` slices or atoms — per-field decision recorded in the PR _(landed: `crates/vize_carton/src/interner.rs` — `Interner<'a>` over the compile arena with a `phf` fast path (`well_known_atom`) covering the HTML/SVG/MathML tags, the Vue built-in components, every normalized directive name and the transform-special-cased attributes, so a `<div>`-heavy template interns its names into `'static` literals without touching the arena or the hash set. **Per-field decision table** — three sources produce an `&'a str`: a **source slice** (the node borrows the template text, zero allocation), an **atom** (one arena copy per distinct computed name per compile), an **arena copy** (`Allocator::alloc_str`, computed and effectively unique). `vize_relief`: `ElementNode::tag` source slice; `AttributeNode::name` source slice; `DirectiveNode::name` atom-by-construction (shorthands normalize to `'static` `bind`/`on`/`slot`, `v-x` forms slice the source, synthesized directives use literals); `DirectiveNode::raw_name` source slice; `TextNode::content` source slice when the decoded run equals its span, arena copy when an entity or whitespace condensing rewrote it; `CommentNode::content` source slice; `SimpleExpressionNode::content` source slice for parsed/attribute expressions, arena copy for rewritten text (prefixing, TS strip, handler wrapping, v-model handlers, Vue-2 filter wrapping), atom for camelized `:foo-bar` shorthand args and v-model prop/event/modifier names; `SimpleExpressionNode::identifiers` / `CompoundExpressionNode::identifiers` follow their content; `CompoundExpressionChild::String` `'static` codegen punctuation; `RootNode::source` the template text; `RootNode::components`/`directives`/`filters` atoms (asset names recur); `ImportItem::path` arena copy; the codegen carriers (`VNodeTag::String`, `DynamicProps::String`, `Callee::String`, `CallArgument::String`, `ArrayElement::String`, `FunctionParams::String`, `FunctionParam::String`, `TemplateLiteralElement::String`, `DirectiveArgumentNode::directive`) are `'static` helper names, element-tag slices, or arena copies of generated text. Vapor IR: `RootIRNode::source` the template text; `templates` arena copies (each assembled once); `component`/`directive` atoms; `SetPropIRNode::tag` / `CreateComponentIRNode::tag` / `DirectiveIRNode::tag` source slices; `DirectiveIRNode::name` `'static` builtin names; `DirectiveIRNode::input_type` a source slice of the `type` attribute; `EventModifiers::{keys, non_keys}` source slices; `IRDynamicInfo::children` and `{Insert,Prepend}NodeIRNode::elements` moved from `std::vec::Vec` into arena vectors. **Deliberately still owned:** `CompilerError::message` (the parser's error vector became `std::vec::Vec<CompilerError>` — diagnostics own their text), `TransformContext`'s working asset sets (frozen to atoms once, at flush), `CodegenContext`'s buffers, every croquis/folio/summary value (P1-5's cross-compile contract), and `vize_musea`'s `ArtVariant::args` (owns `serde_json::Value`, so its vector left the arena). Build-then-freeze: attribute/directive values still accumulate in a parser-side scratch `CompactString` and freeze once (source slice when the accumulated bytes equal their span, arena copy otherwise); decoded **text runs** buffer in `parser/pending_text.rs` and are frozen at the first non-text tokenizer callback, so a run of N entities stays linear instead of recopying per entity)_
- [x] Collapse the P1-1 two-pool handle: flip `vize_carton::{Box, Vec}` aliases to the oxc arena types (their no-`Drop` const assert passes once nodes are string-free), delete the `bump` pool from `Allocator`, and retire the `Bump`/`BumpString`/`BumpVec` re-exports _(landed: `vize_carton::{Box, Vec}` are `oxc_allocator::{Box, Vec}`, `Allocator` is a transparent wrapper over `oxc_allocator::Allocator` with `Deref`/`AsRef` to it, and the `bump` pool, `as_bump`, `Deref<Target = Bump>`, `Bump`, `BumpString` and `BumpVec` are gone along with the workspace `bumpalo` dependency (`StringBuilder` re-exports oxc's arena builder for the build-then-freeze sites). oxc's containers take `&A where A: GetAllocator`, so `&Allocator` gained a `GetAllocator` impl and the ~370 `new_in` call sites pass `&allocator`. The const assert did its job as the enforcer: after the node diet it still caught `Vec<'a, TextPart>` (owned strings in the core text-call carrier) and `Vec<'a, ArtVariant>` (musea variants own `serde_json` values) — both fixed at the source rather than waived. One deliberate escape hatch: `Allocator::alloc_owned`, a `ManuallyDrop`-based park used **only** by the six compile entry points that receive an owned `Croquis` by value and must hand the transform a reference at the arena lifetime. It reproduces exactly what `bumpalo::Bump::alloc` did before (the arena never ran destructors), is documented as such, and does not widen for nodes — decoupling the summary's lifetime from the arena is P1-11's lifetime-contract work)_
- [x] Delete `ensure_sufficient_stack` `Drop` impls: `crates/vize_relief/src/relief/elements.rs`, `relief/control_flow.rs`, `crates/vize_atelier_vapor/src/ir_drop.rs` (arena drop is free once nothing owns heap strings) _(landed: the three `impl Drop` blocks (`ElementNode`, `IfBranchNode`, `ForNode`) and the whole of `ir_drop.rs` (`drop_ir_stack_safe` and its worklist) are deleted, with the call sites in `vize_atelier_vapor::compile`, `vize_atelier_jsx::vapor` and the vapor bench removed. Grep zero: no `impl Drop` on any AST or IR type anywhere in `crates/` (the 26 surviving `impl Drop`s are terminal/LSP/lock RAII guards). `ensure_sufficient_stack` survives only where it always belonged — the recursive **walks** (`condense_whitespace`, the codegen node/helper walks, `hoist_static`, `v_slot`, the legacy desugar, the vapor lowering and template walks, two patina rules) — 39 call sites, none of them teardown)_
- [x] `stress-deep.vue` fixture passes without the stack guard (the guard's reason is gone, prove it) _(landed: `crates/vize_atelier_dom/tests/davinci_teardown_without_stack_guard.rs` and its vapor twin. Each pins both halves: the mechanical one — `!needs_drop::<T>()` for every template node (`TemplateChildNode`, `ElementNode`, `AttributeNode`, `DirectiveNode`, `PropNode`, `TextNode`, `CommentNode`, `InterpolationNode`, `SimpleExpressionNode`, `IfNode`, `IfBranchNode`, `ForNode`, `RootNode`) and every IR node, so teardown has no glue to recurse through — and the end-to-end one: `stress-deep.vue` compiles (dom) / lowers and generates (vapor) and its tree plus arena are dropped **inside a 256 KiB thread**, the frame the retired `Drop` impls used to recurse on. `RootIRNode` is pinned as the one IR value that still runs a destructor (it owns the template maps) and is a single by-value root, not a recursive edge)_
- [x] The `docs/content/architecture/performance.md` interning claim becomes true — note for P1-12 _(the doc is untouched here by design — P1-12 owns the truth pass — but its interning claim is now **backed**: `vize_carton::interner` exists, is wired into the armature parser (camelized shorthand args, reconstructed `v-pre` attribute names), the atelier transform context (v-model prop/event/modifier names, root component/directive/filter asset names) and the vapor lowering context (`.camel` prop keys), and its well-known table resolves the tag/directive/attribute vocabulary to `'static` literals. `crates/vize_carton/README.md` and the three `docs/content/**/stability.md` key-export tables were corrected in this task because they named the retired `Bump` re-export; `performance.md` (and its ja/zh mirrors) still say `Bump::new()` and are left for P1-12)_
- [x] Node-size static asserts re-pinned (`crates/vize_relief/src/relief/{elements,expressions,control_flow,nodes}.rs`). `CompactString` 24 → `&str` 16 per string field, bumpalo `Vec` 32 → oxc `Vec` 24 per arena vector, `Box` unchanged at 8. Old → new: `RootNode` 296→224 (248 under `_legacy`, which carries the extra `filters` vector), `DirectiveNode` 208→176, `ForNode` 176→168, `ElementNode` 128→104, `SimpleExpressionNode` 120→88, `CompoundExpressionNode` 88→64, `AttributeNode` 80→56, `IfBranchNode` 80→72, `CommentNode` 40→32, `TextNode` 32→24. `SourceLocation` stays 8 (P1-3/P1-4)
- [x] Bench re-run (P0-3 recorders, `cargo bench --bench davinci -- --quick`, base = this branch's parent measured on the same machine at the start of the task). **Alloc counts are the trustworthy signal here** — deterministic and machine-independent — and they are the new ratchet. Fused compiles, allocs base → P1-10: dom small 52→39, medium 329→264, large 656→573, stress-deep 669→**426**, stress-wide 255→245, stress-interp 1048→**536**; ssr small 73→60, medium 1099→1030, large 1106→983, stress-deep 612→**369**, stress-wide 416→405, stress-interp 3149→**2637**; vapor small 90→73, medium 588→515, large 1136→1003, stress-deep 764→**514**, stress-wide 280→261, stress-interp 1495→**974**; armature parse small 21→9, medium 171→107, large 350→272, stress-deep 397→**155**, stress-wide 213→204, stress-interp 616→**105**. Wall p50 followed: armature parse −22.7/−12.9/±0.0/−18.1/−9.7/**−23.4**% (small…stress-interp), dom compile −6.8/−6.7/−4.7/−9.2/−9.6/−8.3%, ssr compile −11.8/−6.7/−6.6/−14.4/−13.3/−12.6%, vapor compile −8.2/−23.4/−11.7/−12.1/−9.2/−19.9%, vapor lower −10.4/−12.6/**−35.1**/−0.8/−23.3/−14.6%. The untouched windows (codegen, croquis) hold allocs ±0 everywhere; the three `*_transform_medium` windows print **+1** alloc — the interner's hash set allocating once when `medium.vue` registers its first asset atom, the only alloc this task adds. `--quick` single-sample outliers re-ran and settled (`armature_tokenize_large` +18.1%→+1.7%, `croquis_analyze_full_small` +20.8%→+3.6%); `atelier_dom_codegen_stress-interp` alone kept printing elevated across three runs (+18.9/+11.7/+16.5%) with allocs ±0 while its fused lane is −8.3% with −512 allocs — the after-runs were taken on a machine that had been compiling for hours, so the improvements above are conservative and this window is flagged for P1-12's truth pass rather than explained away. Both JSON sets + logs saved under the task scratchpad (`p1-10-bench/{base,p1-10,rerun}`)

**Acceptance:** grep zero for manual `Drop` on node types; alloc counts drop (pin the number as the new ratchet); corpus parity. _(Met: grep zero above; the alloc table above is the new ratchet; parity is carried by the full suite — 328 suites / 8,842 tests green with insta snapshots untouched, including the SFC differential comparator that dual-runs every admitted rewrite, plus both feature suites (`vize_atelier_core --features legacy`, `vize_vitrine --no-default-features --features wasm`). Corpus verification runs at the orchestrator level.)_
**Deps:** P1-3.

## P1-11 — Arena reuse across files

**Steps:**

- [x] Pool in the CLI batch path (`crates/vize/src/commands/build/`): per-rayon-worker `Allocator` reset between files (`Allocator::reset()`, available in the pinned oxc_allocator), not reallocated _(landed: `crates/vize_carton/src/pool.rs` — a `thread_local` free list of arenas plus a `PooledAllocator` guard that **owns** its arena for one compile and, on drop, resets it and returns it to the worker. **Choice of thread_local over rayon-threaded state, and over a handle passed down from the CLI:** the arena's birth site is several layers below the batch loop (P1-2 lifted it to one `Allocator::new()` per template-block compile inside `vize_atelier_sfc`), a `thread_local` is already exactly "per rayon worker" — the shape `architecture.md` fixes ("each rayon worker owns a pooled arena, so arenas are never shared across threads or files") — it needs no mutex on the acquire path (oxc's own `AllocatorPool` locks a shared stack), and it serves every embedder that compiles a file (CLI batch, LSP, napi) instead of only the caller that could thread a handle through. The ten per-compile birth sites in the SFC lane now `pool::acquire()`: both template-block sites in `compile.rs`, `compile/template_only.rs`, the v-model demotion probe in `compile/helpers.rs`, the three `css.rs` entries, the inline and function-mode script compilers, and `setup_emit`'s css-var transform. Idle arenas are capped at 4 per worker (nesting depth, not a high-water cache), so a worker parks the memory one compile needs, not the batch's peak)_
- [x] **Lifetime contract documented and enforced:** every arena-backed value is consumed or converted to its owned form (stage artifacts, cached results, diagnostics) _before_ reset — caches never hold `&'a` references and never pin an arena (see the architecture arena/cache contract) _(landed: the contract is written down in `crates/vize_carton/src/allocator.rs`'s module docs (the rule and what enforces it), in `pool.rs` (guard ownership, "nothing may hold a guard beyond one file"), in `crates/vize/src/commands/build/runner/compile.rs` (the CLI's file boundary), and in `runner/cache.rs` (the batch's one resident structure). **Enforcement is compile-time first:** `reset` takes `&mut self` and the guard owns its arena, so an arena-backed value that outlived a file is a borrow-check error, not a runtime bug; the owned-form half is pinned by `'static` assertions over everything that crosses the boundary (`SfcCompileResult`, `SfcError`, `StatsCompileCacheKey/Entry/Cache`, `CompileOutput`, `CompileError`, `FileProfile`) — a value holding an `&'a` into arena bytes cannot be `'static`, so the assertion stops compiling. **Audit finding, and the escape hatch resolved:** `Allocator::alloc_owned` parked an owned `Croquis` as `ManuallyDrop` **in the arena**, so the summary's own heap allocations were never released — a per-compile leak that a batch of 36.5k files turns into ~630 MB. It now parks the value in an allocator-owned `Box` (same `&'a` borrow, same six call sites, `&mut T` → `&T`) and `reset`/`Drop` run the real destructor. Unreleased allocations over a 731-file project fell 12,448 → 4,932 (−60.4%). The first draft took the pointer before pushing the box; **Miri rejected it** (moving a `Box` retags its pointee under Stacked Borrows), so the pointer is now taken from the stored box — the one `unsafe` in the change, with the safety argument written out)_
- [x] Escape check: asan/miri lane over the pool (nothing borrows across `reset`), plus a `#[cfg(debug_assertions)]` arena-generation counter that panics on cross-file survivals; includes a **resident-cache reset scenario** (cache populated → arena reset → cache read) proving cached data is owned _(landed, all three. **Generation counter:** `crates/vize_carton/src/allocator/generation.rs` — every arena carries a process-unique id and a generation that `reset` advances; `Allocator::stamp()` records both and `assert_stamp_current` panics with "arena-backed value outlived its compile: stamped arena #N generation G, read against …". Debug-only (the stamp is a ZST in release, validation compiles away). It covers what borrow-checking cannot see, and the pool wires it into two live checks: `pool::acquire` debug-asserts the arena it hands out is empty (`allocated_bytes() == 0` — a pool that skipped a reset would leak file N's bytes into file N+1), and the CLI asserts `pool::checked_out() == 0` at each file boundary in `runner/compile.rs` and `runner/compile_stats.rs` (a guard parked in a cache or `mem::forget`-ed would pin an arena across files). **Resident-cache reset scenario:** twice — `crates/vize/src/commands/build/runner/cache/tests.rs` runs it against the real `--format stats` cache (populate from a compile → 64 other files reuse and overwrite that arena → read back, exact equality, and recompiling reproduces the entry), and `crates/vize_atelier_sfc/tests/davinci_arena_pool.rs` runs it against full SFC compiles, alongside the cold-vs-hot arena byte-identity test. **Miri:** `.github/workflows/miri.yml` gains two steps — `cargo miri test -p vize_carton --lib -- allocator pool` and `cargo miri test -p vize_atelier_sfc --test davinci_arena_pool --no-default-features`. Both green locally (4.8 s and 90.8 s under the interpreter; the SFC scenario scales its file count under `cfg!(miri)`). ASan was not added: the miri lane already executes this exact path end-to-end and reports the aliasing class ASan cannot)_

**Acceptance:** peak RSS on the corpus batch drops (pin as ratchet); asan/miri lane green. _(Met. **New ratchet — corpus batch peak RSS**, `vize build "tests/_fixtures/_git/**/*.vue" --format stats` over all 36,541 committed corpus files, `ci-opt` binaries, `/usr/bin/time -l` maximum resident set size, before = P1-10 `bfd17cb2f`, after = this branch, same machine: **12 workers 766.5 MB → 171.1 MB (−77.7%)**, five runs each (before 753.6–772.8 MB, after 154.0–178.3 MB); **one worker 717.0 MB → 88.2 MB (−87.7%)**, three runs each (before 716.9–717.0 MB, after 88.16–88.18 MB). The single-worker figure is the accumulation signal — it is scheduling-free and shows the peak was per-file leakage, not per-worker arenas. **Alloc deltas** (ant-design-vue, 731 files, `--threads 1` so arena reuse is schedule-independent): pressure 661,392 → 660,210 calls and 427.29 → 398.65 MiB (−28.64 MiB); unreleased allocations 12,448 → 4,932 (−60.4%). Wall time is unchanged within noise (12 workers 4.26–4.66 s → 4.31–6.33 s; one worker 14.85–14.90 s → 14.84–14.94 s) — this is a memory task. **Zero output-byte change** verified at corpus scale: both binaries emitted all 36,541 files and the SHA-256 manifests are identical. JSON + logs under the task scratchpad (`p1-11-bench/`).)_
**Deps:** P1-10.

## P1-12 — Performance-doc truth pass

**Steps:**

- [x] `docs/content/architecture/performance.md`: every claim (interning, arenas, allocation behavior, string handling) rewritten to match shipped code, with numbers from this phase's benches; the stale pre-Davinci claims deleted _(landed, plus the four locale mirrors — `docs/content/{ja,zh-CN,pt-BR,fr}/architecture/performance.md`; **fr exists too**, the task brief listed only three. Every claim in the page was inventoried and classified against the code that would make it true; the table below is the review surface. Claims classed FALSE were deleted, not softened, and where readers needed something in their place the replacement states what the code actually does with a file citation. **Numbers and their conditions:** the arena/expression figures come from this phase's recorded runs (P1-3/5/7/9/10/11 annotations above) over the fixed ladder `tools/benchmarks/crates/davinci_harness/fixtures/{small,medium,large,stress-deep,stress-wide,stress-interp}.vue`; the page states plainly that alloc counts are deterministic and exact while microbench wall times are `--quick` samples from a shared dev machine and therefore directional, and that the Blacksmith reference-runner recordings are still pending — which is why every `wall_p50_ns`/`allocs` entry in `budgets.toml` is still `0` (report-only). **Note for the review:** `tools/benchmarks/results/davinci/*.json` is gitignored (`.gitignore:5`), so those files are local run output, not committed baselines — the acceptance line's "signs off claims against `tools/benchmarks/results/davinci/`" has to be read as "against the recorded per-task numbers in this plan", which is what the page cites. The committed cross-tool artifact `tools/benchmarks/results/tool-benchmark-latest.json` is unaffected and still pins the Vite row in all five locales.)_

**Claim inventory** (claim → old status → new status → evidence):

| Claim                                                                 | Old                         | New                      | Evidence                                                                                                                                                                                                                                                                                              |
| --------------------------------------------------------------------- | --------------------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rust zero-cost abstractions + native threading beat the JS compiler   | TRUE                        | TRUE (kept)              | `tools/benchmarks/results/tool-benchmark-latest.json` `compile` surface                                                                                                                                                                                                                               |
| 15k-SFC table `9.35s`/`3.47s`/`353ms`/`26.0x`, 36.9 MB corpus         | UNVERIFIABLE                | **DELETED**              | undated local run, no wired harness (`tools/benchmarks/scripts/run.ts` is in no script or workflow), no recorded rev; replaced by the committed Blacksmith `compile` surface with runner, commit, date, method                                                                                        |
| Rayon "scales **near-linearly** with CPU core count"                  | UNVERIFIABLE                | **DELETED** (quantifier) | nothing measures linearity; kept as "scales with CPU core count"                                                                                                                                                                                                                                      |
| "Native Batch Scaling Behavior" table (100/1k/5k/15k, 1 vs 12 thread) | UNVERIFIABLE                | **DELETED**              | no harness, no reproduce command, no date; `tools/benchmarks/scripts/scale.mjs` is a different (Vite-plugin) benchmark                                                                                                                                                                                |
| AST nodes avoid per-node heap allocations                             | ASPIRATIONAL                | **NOW TRUE**             | P1-10: node fields are `&'a str`; `crates/vize_relief/src/relief/elements.rs:20,80,111,156`                                                                                                                                                                                                           |
| No GC pauses / no JIT warmup / predictable AOT performance            | TRUE                        | TRUE (kept)              | property of the native target                                                                                                                                                                                                                                                                         |
| Rayon `par_iter` data-parallel compile                                | TRUE                        | TRUE (kept, real code)   | `crates/vize/src/commands/build/runner.rs:144,176-178`                                                                                                                                                                                                                                                |
| Code sample `let arena = Bump::new();` per file                       | FALSE                       | **DELETED**              | bumpalo is not a dependency of any crate (P1-10); no `Bump::new()` anywhere in `crates/`. Replaced with the real driver plus `vize_carton::pool::acquire()` (`crates/vize_atelier_sfc/src/compile.rs:336,658`)                                                                                        |
| Work-stealing keeps load balanced                                     | TRUE                        | TRUE (kept)              | rayon scheduler                                                                                                                                                                                                                                                                                       |
| "`NodeKind` with 20 variants uses a single byte"                      | FALSE                       | **DELETED**              | no `NodeKind` in the AST crate; real type is `NodeType`, `#[repr(u8)]`, 27 variants — `crates/vize_relief/src/relief/core.rs:10-40`                                                                                                                                                                   |
| "Rust automatically reorders struct fields" (generic)                 | TRUE                        | **REPLACED**             | swapped for the repo-specific fact: `const` size asserts pin `ElementNode` 104, `SimpleExpressionNode` 88, `AttributeNode` 56, `TextNode` 24, `SourceLocation` 8 (`relief/{elements,expressions,control_flow,nodes}.rs`)                                                                              |
| No JS object headers                                                  | TRUE                        | TRUE (kept)              | —                                                                                                                                                                                                                                                                                                     |
| "a single, statically-linked executable"                              | FALSE                       | **CORRECTED**            | static only on the 2 musl targets, verified by `tools/commands/ci/github/verify-musl-cli-binary.rs`; glibc/macOS/Windows link dynamically (`tools/commands/ci/github/release-platforms.rs:22-59`), and the Vite path loads `@vizejs/native`, a dynamic Node addon                                     |
| "bump allocator … using **bumpalo**"                                  | FALSE                       | **CORRECTED**            | `crates/vize_carton/src/allocator.rs:74-83` wraps `oxc_allocator`; workspace bumpalo dependency deleted at P1-10                                                                                                                                                                                      |
| Allocation is O(1) (pointer bump)                                     | TRUE                        | TRUE (kept)              | `oxc_allocator` `alloc_layout`, `#[inline(always)]`                                                                                                                                                                                                                                                   |
| "Deallocation is O(1) — **drop the entire arena** when done"          | FALSE                       | **CORRECTED**            | P1-11: `reset()` + per-worker free list, cap 4 — `crates/vize_carton/src/allocator.rs:177-182`, `crates/vize_carton/src/pool.rs:46,63,143-161`; corpus peak RSS 766.5→171.1 MB (12 workers) / 717.0→88.2 MB (1 worker)                                                                                |
| Memory locality is excellent (contiguous nodes)                       | TRUE                        | TRUE (kept)              | arena layout + the size pins above                                                                                                                                                                                                                                                                    |
| Arena-backed values may not outlive their compile                     | —                           | **ADDED**                | `reset(&mut self)` + pool guard; debug generation stamp `crates/vize_carton/src/allocator/generation.rs`                                                                                                                                                                                              |
| No manual `Drop` on AST/IR types                                      | —                           | **ADDED**                | P1-10 grep-zero; enforced by the arena containers' `needs_drop` const assert                                                                                                                                                                                                                          |
| Tokenizer is byte-oriented over a stream                              | TRUE                        | TRUE (kept)              | `crates/vize_armature/src/tokenizer.rs:20` `input: &'a [u8]`                                                                                                                                                                                                                                          |
| No intermediate token arrays                                          | TRUE                        | TRUE (strengthened)      | there is no `Token` type at all; callbacks trait `crates/vize_armature/src/tokenizer/types.rs:75-122`                                                                                                                                                                                                 |
| "parser consumes tokens **lazily**, produced **on demand**"           | FALSE                       | **CORRECTED**            | push-based: `tokenize()` drives to EOF and calls the sink — `crates/vize_armature/src/tokenizer.rs:164-212`; no iterator, no `next_token`                                                                                                                                                             |
| Tokenizer "reduces peak memory usage and improves cache behavior"     | UNVERIFIABLE                | **DELETED**              | no measurement isolates it                                                                                                                                                                                                                                                                            |
| Common strings are interned                                           | ASPIRATIONAL                | **NOW TRUE**             | P1-10 shipped `crates/vize_carton/src/interner.rs:124-154`                                                                                                                                                                                                                                            |
| "interned via **`compact_str`**"                                      | FALSE                       | **CORRECTED**            | interner stores arena `&'a str` via `alloc_str`; `compact_str` survives only in parser scratch buffers, diagnostics (`crates/vize_relief/src/errors.rs:14`) and codegen buffers                                                                                                                       |
| "String comparison is **pointer comparison** (O(1))"                  | FALSE                       | **DELETED**              | atoms are plain `&'a str`; every consumer compares content (`crates/vize_atelier_core/src/codegen/props.rs:30`, `codegen/element/inline.rs:297-298`, `crates/vize_armature/src/parser/element/tags.rs:90`). Replaced by an explicit statement that interning buys allocation savings, not a fast `==` |
| Duplicate strings share one allocation                                | TRUE                        | TRUE (scoped)            | true for interned **computed** names; most names are zero-copy source slices — page now says so                                                                                                                                                                                                       |
| Known-string hash lookups are compile-time computed                   | TRUE                        | TRUE (quantified)        | `phf_set!` of 181 entries, `crates/vize_carton/src/interner.rs:36-79`                                                                                                                                                                                                                                 |
| Vite plugin caches per file; only modified files recompile            | TRUE                        | TRUE (kept)              | `npm/builder/vite/src/plugin/compiled-module-cache.ts:27`                                                                                                                                                                                                                                             |
| "The cache key is the **file content hash**"                          | FALSE                       | **CORRECTED**            | in-memory dev/HMR cache is keyed by resolved path with eviction on hot update (`plugin/hmr.ts:98-101`); the recompile gate is `mtime`+size (`crates/vize_atelier_sfc/src/vite_plugin/precompile.rs:30-35`); only the on-disk cache is SHA-256 (`plugin/precompile-cache-key.ts:45-47`)                |
| Linter / formatter / type-checker tables                              | TRUE, conditions incomplete | TRUE (conditions added)  | unchanged numbers, now labelled "local workstation" with the machine table kept as their conditions                                                                                                                                                                                                   |
| Vite plugin row + retraction paragraph                                | TRUE                        | TRUE (untouched)         | pinned by `tests/tooling/docs-vite-benchmark-row.test.ts` in all five locales                                                                                                                                                                                                                         |
| Phase-1 alloc ratchet, node sizes, peak RSS, parse-once payback       | —                           | **ADDED**                | new "Measured" section; numbers from P1-7/P1-10/P1-11 records above                                                                                                                                                                                                                                   |

**Needs a code change (not done here — this task is docs-only):** two in-code doc comments still name the removed allocator — `crates/vize_relief/src/relief.rs:4` ("allocated in a bumpalo arena") and `crates/vize_atelier_core/src/codegen/v_if.rs:116` ("require bumpalo allocation"). Neither affects behavior; both are stale prose that P1-13's grep-zero sweep should catch.

**Open for the reviewer:** (a) the 15k-SFC table now restates the committed Blacksmith `compile` surface rather than the retired local numbers — no test pins it, so a pin like `docs-vite-benchmark-row.test.ts` would stop it drifting; (b) the Blacksmith snapshot itself is from vize 0.303.0 (2026-07-30), i.e. pre-Davinci, which the page says outright — a re-record is the real fix and is not P1-12's to do.

**Acceptance:** review point — maintainer signs off claims against `tools/benchmarks/results/davinci/`.
**Deps:** P1-10, P1-11.

## P1-13 — Phase exit

Evaluated 2026-08-15 at this branch. Phase 0's pattern governs: a line is
ticked only when it is satisfied and an unticked line names its blocker; no
line's wording was softened to make it tickable. **Two of six tick.**

- [x] **Corpus compile parity: byte-identical, waiver ledger empty** — `rust-script tools/commands/davinci/corpus-diff.rs --shards 2 --timeout-ms 600000` at this tree (release `vize`, corpus `node_modules` removed first — every sweep re-materializes them and stale ones corrupt hashes): `corpus-diff: PASS in 552s — zero gating drift across 568 rows (142 projects x 4 surfaces, 151464 files); scope proof matches 142-project manifest`. **Waiver ledger: empty** — no corpus waiver was opened anywhere in phase 1. P1-9 ran an explicit zero-waiver budget on the most output-visible surface and held it (801 305 dual-run rewrite comparisons, zero divergence); P1-8's differential came back divergent _before_ a deletion existed, so its two resolutions (`scanner-parity-report.md`) are proposals awaiting review, not ledger entries. The single drifting row, `typechecker/element-plus`, is the phase-0 filed nondeterminism in `corpus-baseline-unstable.json` — a quarantined unstable row, non-gating by design, not a waiver.
- [x] **`davinci.expr.parses` counter law asserted in CI** — two ordinary integration tests, both inside the default `cargo test --workspace` CI runs (`.github/workflows/check.yml:386`), pinning the two distinct laws that share the counter's name (the split is documented in `expr-reparse-baseline.md`). `crates/vize_armature/tests/davinci_expr_parses.rs` pins the **P1-5 parse law**: one template parse attempts exactly one retained oxc parse per distinct non-static expression (`samples == total == 9` on a nine-expression template covering every retained shape), the AST is retained exactly where the content is one complete expression, and guard-refused text records **zero** increments — i.e. "parsed at most once, always", which under parse-at-template-parse is `counter == distinct expressions`. `crates/vize_atelier_{dom,ssr,vapor}/tests/davinci_expr_reparse_floor.rs` pin the **P1-7 legacy re-parse floor** with exact equality per ladder fixture (dom `0,0,16,0,0,0`; ssr `0,0,27,0,0,0`; vapor `0,0,8,0,0,0` — the surviving cells are `large.vue`'s kept-with-reason synthesized-text classes). Stated plainly, because the gate should not be read wider than it is: CI pins both laws on the six-fixture ladder; the corpus-scale evidence that no consumer re-parses is the P1-6/P1-7 differential lanes, which are feature-gated and run locally, not in CI.
- [ ] **Compile bench improvement ≥ target pinned at phase start** — **no target was ever pinned, so there is nothing to measure against and the line cannot be ticked.** The task text required recording it in `budgets.toml` before P1-5 merged; `budgets.toml` carries no phase-1 improvement target, and inventing one now that the measured results happen to beat would be self-grading, which the assurance doctrine forbids. A second, compounding miss: **no phase-start baseline was committed either** — `tools/benchmarks/results/davinci/baseline/` has never existed in the tree (`.gitignore:5` ignores the run output and the comment promises committed baselines under `baseline/`), and all 85 `[bench]` entries still carry the `wall_p50_ns = 0` seed, so `bench-compare.mjs` reports every bench `unbaselined`/`unrecorded`. This is P0-4's still-open pending surfacing at a phase gate. What exists instead, for the maintainer to judge against their own bar: (a) the fresh phase-end run at this tree, recorded as the `allocs` field of every `[bench]` entry in `budgets.toml`, with wall p50 in the task scratchpad JSONs; (b) `armature_parse` allocations phase-start → phase-end, phase-start quoted from P1-3's committed "before" column (the P1-2 tree — P1-1/P1-2 were plumbing recorded within noise; this is the closest committed stand-in, **not** a re-measured phase-start run): small 25 → 9, medium 236 → 107, large 479 → 272, stress-deep 517 → 155, stress-wide 321 → 204, stress-interp 620 → 105; (c) the only same-machine cross-phase wall measurement anyone took — P1-7's re-bench of the pre-P1-5 tree (rev `67a9c2d50`), giving fused ssr `stress-interp` 346.8 µs → 299.8 µs (**−13.6%**) while dom and vapor `stress-interp` still carry P1-5's parse cost (+17.2% / +23.0%) because those lanes had no legacy re-parses to delete; (d) every task's own before/after record above. **To close this properly**: record the P0-4 reference-runner baselines on Blacksmith, then re-bench the phase-start rev (`2ed872e0b`, the mainline commit P1-1 landed on top of) and this tree on that runner — a local dev machine cannot produce a wall number worth gating on.
- [ ] **Alloc count / peak RSS improvements pinned as new ratchet baselines** — **the alloc half is done and enforced; the RSS half has nowhere to live yet.** Alloc counts are deterministic and machine-independent, so they were fillable here without the reference runner: all 85 `[bench]` entries in `budgets.toml` now carry a real `allocs` value from a fresh `cargo bench --bench davinci -- --quick` at this tree (JSONs + logs in the task scratchpad), with `wall_p50_ns` and `rss_peak_bytes` left at the `0` seed and the field docs updated to say what the new state means. **The tool could not express "gate allocs, ignore wall" — fixed in this task.** As first measured: `rust-script tools/commands/davinci/bench-compare.rs` → `breaches=0 gated_ok=0 report_only=85 registered=85`, every row `unbaselined`, because `tools/commands/davinci/bench-compare.rs` short-circuited an entry to report-only twice before any comparison ran (no baseline report; `budget.wall_p50_ns === 0`), and even past both guards its alloc gate compared the current report against the **baseline report**, never against `budget.allocs` — a field the loader validated and nothing read. Two of the three named changes landed: the alloc gate now evaluates **independently of the wall side** and compares against `budget.allocs`, so the normative file is the authority its ratchet header claims. A bench whose wall baseline is still unrecorded reports as `alloc-gated` and its alloc count is enforced exactly; a new bench must land with its measured count, because a seeded `0` now fails loudly instead of going silently ungated (`tests/tooling/davinci-bench-compare.test.ts` pins all of it, including an alloc breach caught with no reference-runner baseline anywhere). The third change — committing the baseline reports — still belongs to the Blacksmith recording. **Peak RSS has no home at all**: the per-bench `rss_peak_bytes` field is process-RSS growth for one microbench (documented report-only, too noisy to gate), which is not the quantity P1-11 moved; the corpus-batch figure belongs in `[resource]`, which is reserved for P5-11 and explicitly "Populated by P5-11". Until it exists, P1-11's numbers live only in this file: **766.5 MB → 171.1 MB (−77.7%, 12 workers), 717.0 MB → 88.2 MB (−87.7%, one worker)** over 36 541 files, `/usr/bin/time -l` maximum resident set size, five/three runs each, `ci-opt` binaries.
- [ ] **Scanner split, string-rewrite prefixing, manual `Drop`s: deleted (grep zero)** — **two of three are clean.** **Manual `Drop`s — DONE, grep zero.** No `impl Drop` on any AST or IR type (`grep -rn "impl.*Drop for" crates/vize_relief/src crates/vize_armature/src crates/vize_atelier_core/src crates/vize_atelier_vapor/src` → **0**); the surviving `impl Drop` entries in `crates/*/src` are RAII guards or test-only witnesses, and `ir_drop.rs` is gone. **Scanner split — DONE:** `identifiers/fast.rs`, `identifiers/slow.rs`, and `needs_ast_extraction` are gone; `identifiers/ast.rs` is the single OXC walk implementation, with the remaining keyword/literal and order/multiplicity/offset raw-surface differences admitted by `scanner-parity-report.md` option 2. **String-rewrite prefixing — PARTIALLY deleted.** On the transform path the retired end-to-start rewriter is out of production — both admitted and legacy inputs now emit through `steps/expression/splice.rs`, and the old loop survives only as that file's `#[cfg(test)]` oracle and as the `cfg(any(test, feature = "davinci-differential"))` comparator in `retained_rewrite.rs`. Two production string-rewrite emitters remain: `codegen/expression/prefix_visitor.rs::apply_rewrites` (descending `replace_range` loop — AST-driven since P1-7, but not the splicer) and `codegen/slots/params.rs:62` (`insert_str` over the synthesized slot-default arrow, a P1-7 kept-with-reason class). Behind them, `steps/expression/reparse.rs` still runs the legacy re-parse chain for P1-9's non-admitted inputs. Deleting it requires shrinking those classes (a wider retained contract), not a sweep.
- [x] **In-phase fallback flags removed (charter #26)** — **none were ever introduced, and the one chartered transitional structure is gone.** Charter #26 governs "the replaced lane stays behind a flag for emergency escape"; the test is whether a flag lets production select the old lane. Full inventory of Davinci-introduced switches: the `davinci-differential` cargo feature (`vize_croquis`, `vize_atelier_core`, `vize_atelier_vapor`, `vize_atelier_sfc`) and the `VIZE_DAVINCI_DIFFERENTIAL_CORPUS` env var that widens its sweep. Neither is an escape hatch — with the feature **on** both paths run and any divergence **panics**; with it **off** none of the comparator code compiles and only the new path exists, so there is no configuration in which production falls back. They are verification lanes, and P1-6 and P1-7 both fix their life in writing as "for one release" (repeated in `crates/vize_croquis/src/drawer/differential.rs`'s module docs); a phase exit is not a release boundary, and deleting them here would destroy the only mechanism that proves P1-9's zero-waiver corpus claim, so they are deliberately **kept** — retirement condition: one release after P1-7, or when P1-8's deletion lands, whichever the maintainer takes first. Every other cargo feature in the workspace predates Davinci or is a platform/product lane (`legacy`/`_legacy`, `native`, `napi`, `wasm`, `glyph`, `maestro`, `profiling`). The chartered transitional states phase 1 did invoke were structural, not flags: P1-1's two-pool allocator handle (deleted at P1-10 — grep zero for `as_bump`, `BumpString`, `BumpVec`; `bumpalo` survives in `Cargo.lock` only as a transitive dep of `corsa_core` and `wasm-bindgen-macro-support`) and P1-5/P1-7's booked parse-cost debt (a number, not a switch). The surviving `retained`/`reparse` fallback **classes** are not flags either — they are inputs the retained contract genuinely does not cover, tracked by the line above.

**Grep-zero sweep, remaining `bumpalo` prose** (this task fixed the three stale code comments — `vize_relief/src/relief.rs`, `vize_atelier_core/src/codegen/v_if.rs`, `vize_glyph/src/script.rs`): two deliberate historical references stay in `vize_carton/src/allocator.rs` (they narrate the P1-1 → P1-10 transition and are accurate as history). **Still false, and out of this task's stated scope** — the shipped dependency table in `docs/content/{,ja,zh-CN,pt-BR,fr}/architecture/overview.md` still lists bumpalo as `vize_carton`'s arena dependency (it is not a dependency of any crate since P1-10), and `davinci-road/architecture.md:46` says the arena is "`oxc_allocator` (bumpalo underneath)", which P1-1 established is untrue at the pinned rev. Both are one-cell corrections for a follow-up docs pass.
