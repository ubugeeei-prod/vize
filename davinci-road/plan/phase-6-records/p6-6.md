# P6-6 — Volt output-target exercise

**Mechanical run landed 2026-09-23. Review point open.** The guest in
`examples/volt-target` answers the P6-1c `output-target` world with one
HEEx module. It is not Volt, and the Volt maintainer has not signed off,
so the phase index stays open.

## Recorded command

From the repository root, with the workspace toolchain (`rust-toolchain.toml`,
`wasm32-wasip2` installed):

```bash
cargo test -p vize_extension_host --offline --features extension-host --test volt_target
```

The test builds the guest, serves it out of process and in process, and
accepts the document through `OutputSession`. The committed emit page is
byte-equal to the guest's answer. `Hello` in the HEEx is linked to bytes
0:5 of the S2 page.

## Contract-change list

| finding | status | resolution |
| --- | --- | --- |
| HEEx text and a span link fit `emit-document-page@1` | resolved | no world change; the golden page round-trips through the host |
| one HEEx module per `emit` call | resolved | a second module is a second call; a multi-file result would be a new world |
| authored path is not a field of `emit-request` | deferred | a new record field is breaking under the P6-8 policy; the S2 page text carries the module name until a major bump |
| the host does not parse `S2Folio` or an S3 document | deferred | P6-1c left the pages opaque; this guest reads its own probe text. Parsing them in the host is a later consumer, not a change this exercise needs |
| Elixir diagnostics | resolved | the existing diagnostic list already requires spans inside authored links; this probe emits none |

## Review point

Requires the Volt maintainer's participation and sign-off. That sign-off is
not in this commit. The index box stays `[ ]`.
