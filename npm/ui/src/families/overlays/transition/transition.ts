export { createTransition, motionDurationMs, useTransition } from "./transition-runtime.ts";

/** Accessible, unstyled transition host that auto-completes CSS motion. */
export { default as Transition } from "./transition.vue";

export type { TransitionController, TransitionOptions } from "./transition-types.ts";
