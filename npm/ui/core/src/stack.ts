export type {
  StackAlign,
  StackAxis,
  StackElement,
  StackExpose,
  StackFlexDirection,
  StackGap,
  StackJustify,
  StackResolvedLayout,
  StackSlotState,
  StackState,
  StackStyle,
} from "./stack-types.ts";

/** Headless, CSS-first single-axis stack powered by native flexbox. */
export { default as Stack } from "./stack.vue";
