export type { SeparatorElement, SeparatorExpose, SeparatorOrientation } from "./separator-types.ts";

/** Native, unstyled separator for thematic breaks and pane boundaries. */
export { default as Separator } from "./separator.vue";
