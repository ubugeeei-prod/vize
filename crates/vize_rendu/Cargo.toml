[package]
name = "vize_rendu"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Rendu - Frontend-neutral owned render HIR for Vize"

[features]
# Transitional feature name retained for workspace feature resolution. Rendu's
# production model has no legacy-frontend dependency or conditional shape.
legacy = []

[dependencies]
vize_atlas.workspace = true
vize_carton.workspace = true

[dev-dependencies]
criterion.workspace = true

[[bench]]
name = "rendu"
harness = false
