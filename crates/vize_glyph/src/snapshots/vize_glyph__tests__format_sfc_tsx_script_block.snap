---
source: crates/vize_glyph/src/lib.rs
expression: result.code.as_str()
---
<script setup lang="tsx">
const Card = (props: { title: string; items: string[] }) => (
  <section class="card">
    <h2>{props.title}</h2>
    {props.items.map((item) => (
      <span key={item}>{item}</span>
    ))}
  </section>
);
</script>

<template>
  <Card title="Docs" :items='["a"]' />
</template>
