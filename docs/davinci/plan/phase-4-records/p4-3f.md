# P4-3f — Provide/inject and race fact groups

**Checked (2026-09-23).** `ProvideInject` and `RaceConditions` publish the
tracker rows in registration order. External readers demand those groups.
The fields are producer storage, gated `provide-inject` and `race-conditions`.

## Pairing

A template usage resolves through `imported_render_target` (P4-3b import
identity, re-exports followed). A tag with no import still uses the
component index, so existing name-matched fixtures keep their parent.
`an_aliased_import_pairs_through_the_imported_file` pairs `ShopButton`
from `./shop/Button.vue` with `Provider.vue`, not `admin/Button.vue`.

## Acceptance

| check                                        | result                                                                        |
| -------------------------------------------- | ----------------------------------------------------------------------------- |
| drawn SFC spec                               | 1 artifact, 2 facts (provide + inject), zero divergences                      |
| race corpus (`ant-design-vue`, `create-vue`) | artifacts=773 compared=15 facts=17                                            |
| provide/inject on that shard                 | compared=0; those `provide()` calls are outside the tracker's top-level shape |
| `provide_inject` library tests               | 44 passed                                                                     |
| `race_condition` library tests               | 9 passed                                                                      |
| `vize` `cross_file_opt_in`                   | 8 passed                                                                      |
| doctor TUI                                   | 17 passed                                                                     |
| TS-35 `fact_demand`                          | 6 passed                                                                      |
| TS-12 matrices                               | 10 pass; consumption `--check` up to date                                     |
