# P4-8a — Neutral-core rule wave

**Not checked.** This installment puts the script registry on JSX. The
template ports, the CSS rules, the remaining script fixtures, and the
witness-exemption drain are still open.

## What landed

`Linter::lint_jsx` calls `lint_jsx_script`, which runs
`append_builtin_script_rules_for_source` on the JSX program. The SFC
script path is unchanged.

The regenerated matrix: SFC∩JSX **221** (was 150). JSX lanes gain
`script` **71**. That is every registered script rule, including the 42
neutral-core script rules.

## TS-39

`script_parity_fixtures_report_the_same_diagnostics` compares
`tests/fixtures/parity/<rule>/{sfc.vue,jsx.tsx}` with `expected.txt`.
Ten rules agree, messages exact:

- `script/no-arrow-functions-in-watch`
- `script/no-async-in-computed`
- `script/no-deprecated-dollar-listeners-api`
- `script/no-deprecated-events-api`
- `script/no-get-current-instance`
- `script/no-internal-imports`
- `script/no-multiple-slot-args`
- `script/no-potential-component-option-typo`
- `script/no-reactive-destructure`
- `script/prefer-import-from-vue`

`lint_script_runs_script_rules` passed. `jsx_heading_has_content` 3
passed. `davinci-matrices.test.ts` 10 pass.

## Still open

The exemption inventory did not shrink. Those script rules still report
legacy exemptions; a witness is a later installment. Fifteen neutral
template rules are still off the markup facade. Seven neutral CSS rules
still do not run from `lint_jsx`.
