---
title: "use-mounted"
description: "Whether the calling component has mounted."
---

<!-- Generated by npm/ui/scripts/generate-reference-docs.ts from the catalogs and source TSDoc. Do not edit by hand. -->

# use-mounted

Whether the calling component has mounted.

|  |  |
| --- | --- |
| Package | `@vizejs/composable/use-mounted` |
| Own the source | `vize lib pull composable:use-mounted` |
| Runtime exports | `useMounted` |
| Gzip budget | 768 B |

## Usage

```ts
import { useMounted } from "@vizejs/composable/use-mounted";
```

## Runtime contract

| Utility | Category | Stability | SSR | Hydration | Cleanup | Targets | Host globals | Uses |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `useMounted` | lifecycle | experimental | safe | stable | none | web, server, worker, native, desktop, terminal | — | — |

## API

### `useMounted`

Whether the calling component has mounted. `false` during server rendering and during hydration, `true` from the component's `onMounted` on: gate client-only rendering behind it to keep hydration stable. Outside a component (no instance to mount) it stays `false`. Uses only public lifecycle hooks, so it works in Vapor components too.

```ts
function useMounted(): Readonly<ShallowRef<boolean>>
```

```ts
const mounted = useMounted();
// <ClientOnlyChart v-if="mounted" />
```
