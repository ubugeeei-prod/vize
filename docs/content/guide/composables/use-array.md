---
title: "use-array"
description: "Reactive `Array.prototype.filter`."
---

<!-- Generated by npm/ui/scripts/generate-reference-docs.ts from the catalogs and source TSDoc. Do not edit by hand. -->

# use-array

Reactive `Array.prototype.filter`.

|  |  |
| --- | --- |
| Package | `@vizejs/composable/use-array` |
| Own the source | `vize lib pull composable:use-array` |
| Runtime exports | `useArrayFilter`, `useArrayMap`, `useArrayFind`, `useArrayReduce`, `useArraySome`, `useArrayEvery`, `useArrayUnique` |
| Gzip budget | 1280 B |

## Usage

```ts
import { useArrayFilter, useArrayMap, useArrayFind, useArrayReduce, useArraySome, useArrayEvery, useArrayUnique } from "@vizejs/composable/use-array";
```

## Runtime contract

| Utility | Category | Stability | SSR | Hydration | Cleanup | Targets | Host globals | Uses |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `useArrayFilter` | state | experimental | safe | stable | none | web, server, worker, native, desktop, terminal | — | — |
| `useArrayMap` | state | experimental | safe | stable | none | web, server, worker, native, desktop, terminal | — | — |
| `useArrayFind` | state | experimental | safe | stable | none | web, server, worker, native, desktop, terminal | — | — |
| `useArrayReduce` | state | experimental | safe | stable | none | web, server, worker, native, desktop, terminal | — | — |
| `useArraySome` | state | experimental | safe | stable | none | web, server, worker, native, desktop, terminal | — | — |
| `useArrayEvery` | state | experimental | safe | stable | none | web, server, worker, native, desktop, terminal | — | — |
| `useArrayUnique` | state | experimental | safe | stable | none | web, server, worker, native, desktop, terminal | — | — |

## API

### `useArrayFilter`

Reactive `Array.prototype.filter`. A type-guard predicate narrows the element type.

```ts
function useArrayFilter<Item, Narrowed extends Item>( list: MaybeRefOrGetter<readonly Item[]>, predicate: (item: Item, index: number, array: readonly Item[]) => item is Narrowed, ): ComputedRef<Narrowed[]>
```

```ts
const active = useArrayFilter(users, (user) => user.active);
```

### `useArrayFilter`

```ts
function useArrayFilter<Item>( list: MaybeRefOrGetter<readonly Item[]>, predicate: ArrayCallback<Item, boolean>, ): ComputedRef<Item[]>
```

### `useArrayFilter`

```ts
function useArrayFilter<Item>( list: MaybeRefOrGetter<readonly Item[]>, predicate: ArrayCallback<Item, boolean>, ): ComputedRef<Item[]>
```

### `useArrayMap`

Reactive `Array.prototype.map`.

```ts
function useArrayMap<Item, Mapped>( list: MaybeRefOrGetter<readonly Item[]>, mapper: ArrayCallback<Item, Mapped>, ): ComputedRef<Mapped[]>
```

```ts
const names = useArrayMap(users, (user) => user.name);
```

### `useArrayFind`

Reactive `Array.prototype.find`. A type-guard predicate narrows the result.

```ts
function useArrayFind<Item, Narrowed extends Item>( list: MaybeRefOrGetter<readonly Item[]>, predicate: (item: Item, index: number, array: readonly Item[]) => item is Narrowed, ): ComputedRef<Narrowed | undefined>
```

```ts
const admin = useArrayFind(users, (user) => user.role === "admin");
```

### `useArrayFind`

```ts
function useArrayFind<Item>( list: MaybeRefOrGetter<readonly Item[]>, predicate: ArrayCallback<Item, boolean>, ): ComputedRef<Item | undefined>
```

### `useArrayFind`

```ts
function useArrayFind<Item>( list: MaybeRefOrGetter<readonly Item[]>, predicate: ArrayCallback<Item, boolean>, ): ComputedRef<Item | undefined>
```

### `useArrayReduce`

Reactive `Array.prototype.reduce`. With an initial value the result has the accumulator type. Without one, the first item seeds the accumulator and an empty array yields `undefined` (instead of throwing like `reduce`).

```ts
function useArrayReduce<Item, Accumulator>( list: MaybeRefOrGetter<readonly Item[]>, reducer: ( accumulator: Accumulator, item: Item, index: number, array: readonly Item[], ) => Accumulator, initial: MaybeRefOrGetter<Accumulator>, ): ComputedRef<Accumulator>
```

```ts
const total = useArrayReduce(cart, (sum, line) => sum + line.price, 0);
```

### `useArrayReduce`

```ts
function useArrayReduce<Item>( list: MaybeRefOrGetter<readonly Item[]>, reducer: (accumulator: Item, item: Item, index: number, array: readonly Item[]) => Item, ): ComputedRef<Item | undefined>
```

### `useArrayReduce`

```ts
function useArrayReduce<Item>( list: MaybeRefOrGetter<readonly Item[]>, reducer: (accumulator: unknown, item: Item, index: number, array: readonly Item[]) => unknown, ...initial: readonly [] | readonly [MaybeRefOrGetter<unknown>] ): ComputedRef<unknown>
```

### `useArraySome`

Reactive `Array.prototype.some`.

```ts
function useArraySome<Item>( list: MaybeRefOrGetter<readonly Item[]>, predicate: ArrayCallback<Item, boolean>, ): ComputedRef<boolean>
```

```ts
const hasErrors = useArraySome(fields, (field) => field.error !== null);
```

### `useArrayEvery`

Reactive `Array.prototype.every`.

```ts
function useArrayEvery<Item>( list: MaybeRefOrGetter<readonly Item[]>, predicate: ArrayCallback<Item, boolean>, ): ComputedRef<boolean>
```

```ts
const allDone = useArrayEvery(tasks, (task) => task.done);
```

### `useArrayUnique`

Reactive de-duplication that keeps the first occurrence of each item. Without `isSame`, items are compared with `SameValueZero` (like `Set`) in linear time; with it, pairwise in quadratic time.

```ts
function useArrayUnique<Item>( list: MaybeRefOrGetter<readonly Item[]>, isSame?: (left: Item, right: Item) => boolean, ): ComputedRef<Item[]>
```

```ts
const tags = useArrayUnique(allTags);
const people = useArrayUnique(rows, (left, right) => left.id === right.id);
```
