---
title: テストとフィードバック
---

<!-- Generated translation; source: guide/testing.md -->

# テストとフィードバック

Vize は**現実世界のテスト**フェーズにあります。焦点は正確さであり、現実世界のプロジェクトは
テストスイート。このページはテスター向けです - Vize の機能を検査する方法、どこを確認するか、どのように行うか
調査結果のレポート、パフォーマンスの測定方法、プロジェクトをテストベッドとして提供する方法。

## プレイグラウンドで検査する

プレイグラウンドには、公式 Vue SFC コンパイラーを並べて表示する**インスペクター**が同梱されています
出力、Vize のコンパイラ出力、生成された仮想 TS、VIR、およびローカルのクロスファイル グラフ
バッチ。これは、特定の項目について Vize が Vue とどこに同意するか、どこに同意しないかを正確に確認する最も速い方法です。
`.vue` ファイル。

- <https://vizejs.dev/play/?tab=inspector> で開きます。
- 各サーフェスの意味については、[Compiler Inspector](./compiler-inspector.md) ガイドを参照してください。

プレイグラウンド インスペクター リンクは、優れた修正再現を実現します。

## テストケースを読む

Vize はさまざまな方法で厳しくテストされています - コンパイラ フィクスチャを公式のものと比較
Vue コンパイラ、`vue-tsc` に対する型チェック パリティ、lint およびフォーマッタ スナップショット、SSR codegen
スナップショット、ファズハーネス、現実世界のアプリケーションフィクスチャ。報告書を提出する前に、多くの場合、
既存のケースをざっと読むのに役立ちます。

- `tests/` および各クレートの `src/snapshots/` の下のコンパイラーおよび SFC パリティ フィクスチャとスナップショット。
- `tests/_fixtures/` の下にある実際のアプリケーション フィクスチャ (例: Elk、Misskey、Nuxt UI、
  E2E と VRT を推進する Reka UI、VOICEVOX)。

ケースが欠落している場合、または結果が間違っているように見える場合、それがまさにこのフェーズで必要とされる種類のフィードバックです。

## 調査結果を報告する

- **プレーンテキストでも問題ありません。**あなたが何をしたのか、何を期待していたのか、何が起こったのかについての明確な説明
  すでに価値があります。
- **可能であれば、最小限の複製**- 最小の `.vue` ファイル (または
  小さなプロジェクト）、それでも問題が発生します。プレイグラウンドインスペクターリンクはうまく機能します。
- 修正レポート、複製、ベンチマーク結果、互換性に関する調査結果はすべて役に立ちます。を参照してください。
  [寄稿](../contributing.md) ガイドと
  [サポート](https://github.com/ubugeeei-prod/vize/blob/main/SUPPORT.md)。

## パフォーマンスを測定する

Vize には**プロファイリング モード**が組み込まれているため、推測する代わりに時間の経過を測定できます。

- ローカル開発では、ツールチェーンはパーサー、コンパイラー、分析、および
  タイプチェックフェーズ。
- CLI にもそれがあります: `vize check --profile` は**vize_curator**を通じてチェックを実行し、
  フェーズごとのプロファイリング レポート。自分自身のパフォーマンス数値を取得して共有するために使用します。
  コードベース。

## プロジェクトをテストベッドとして提供する

実際のかなり大きなコードベースでは、合成サンプルでは決して検出されない失敗が見つかります。**ライセンスの場所
これを許可すると、プロジェクトを Vize のフィクスチャに追加して E2E / VRT ターゲット**にすることができるため、将来的には
回帰は自動的に検出されます。

Vue アプリケーション、ライブラリ、フレームワーク、または使用できるツールを保守している (または知っている) 場合は、これを使用してください。
ぜひお知らせください - 修正リクエストをオープンするか、ご連絡ください。コードベースが大きくなり、より現実的なものになると、
信号の方が便利です。
