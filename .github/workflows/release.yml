name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

env:
  CARGO_INCREMENTAL: 0
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PUBLISH_RETRY_LIMIT: 6
  PUBLISH_RETRY_DELAY: 10
  PUBLISH_RESOLUTION_RETRY_LIMIT: 24
  PUBLISH_RESOLUTION_RETRY_DELAY: 10
  NPM_TAG: ${{ contains(github.ref_name, '-alpha') && 'alpha' || contains(github.ref_name, '-beta') && 'beta' || contains(github.ref_name, '-rc') && 'rc' || 'latest' }}

jobs:
  plan-release-platforms:
    name: Plan release platforms
    runs-on: blacksmith-32vcpu-ubuntu-2404
    timeout-minutes: 5
    outputs:
      include_slow_platforms: ${{ steps.platforms.outputs.include_slow_platforms }}
      release_minor: ${{ steps.platforms.outputs.release_minor }}
      cli_matrix: ${{ steps.platforms.outputs.cli_matrix }}
      native_matrix: ${{ steps.platforms.outputs.native_matrix }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Select release platforms
        id: platforms
        run: node tools/github/release-platforms.mjs github-output "${{ github.ref_name }}"

  # Build CLI binaries for all platforms
  build-cli:
    name: Build CLI - ${{ matrix.settings.target }}
    needs: plan-release-platforms
    runs-on: ${{ matrix.settings.host }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        settings: ${{ fromJSON(needs.plan-release-platforms.outputs.cli_matrix) }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: ./.github/actions/setup-moonbit

      - name: Enable long paths (Windows)
        if: runner.os == 'Windows'
        run: git config --system core.longpaths true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          targets: ${{ matrix.settings.target }}

      - name: Install Rust target
        run: rustup target add ${{ matrix.settings.target }}

      - name: Setup Wild linker (Linux)
        if: runner.os == 'Linux'
        uses: wild-linker/action@0bbbfa5df4380cab8e63cb8505a1ce65e1d10203 # v0.9.0
        with:
          wild-version: "0.9.0"

      - name: Install cross-compilation tools (Linux ARM64)
        if: matrix.settings.target == 'aarch64-unknown-linux-gnu'
        run: moon run --target native - -- < tools/moon/scripts/github/install_cross_compile_tools.mbtx

      - name: Cache Rust
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          shared-key: release-cli-${{ matrix.settings.target }}
          cache-workspace-crates: "true"
          save-if: "false"

      - name: Tune Windows release profile
        if: runner.os == 'Windows'
        shell: bash
        run: |
          echo "CARGO_PROFILE_RELEASE_LTO=thin" >> "$GITHUB_ENV"
          echo "CARGO_PROFILE_RELEASE_CODEGEN_UNITS=16" >> "$GITHUB_ENV"

      - name: Build CLI
        shell: bash
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
        run: cargo build --release -p vize --target ${{ matrix.settings.target }}

      - name: Create archive (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: moon run --target native - -- ${{ matrix.settings.target }} ${{ matrix.settings.archive }} vize < tools/moon/scripts/github/create_cli_archive.mbtx

      - name: Create archive (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: moon run --target native - -- ${{ matrix.settings.target }} ${{ matrix.settings.archive }} vize.exe < tools/moon/scripts/github/create_cli_archive.mbtx

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cli-${{ matrix.settings.target }}
          path: ${{ matrix.settings.archive }}
          if-no-files-found: error
          compression-level: 0

  # Package editor extensions for GitHub Releases and registry validation
  build-editor-extensions:
    name: Build Editor Extensions
    runs-on: blacksmith-32vcpu-ubuntu-2404
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: false

      - name: Install JS dependencies
        run: vp install --workspace-root --frozen-lockfile --prefer-offline

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          shared-key: release-tools
          cache-workspace-crates: "true"
          save-if: "false"

      - name: Package VS Code extension
        run: vp run --workspace-root package:vscode-extension

      - name: Check Zed extension
        run: vp run --workspace-root check:zed-extension

      - name: Package Zed extension source
        run: vp run --workspace-root package:zed-extension

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: editor-extensions
          path: |
            emacs-vize-extension.tar.gz
            helix-vize-extension.tar.gz
            npm/vscode-vize/dist/vize.vsix
            nvim-vize-extension.tar.gz
            vim-vize-extension.tar.gz
            zed-vize-extension.tar.gz
          if-no-files-found: error
          compression-level: 0

  # Publish VS Code extension when VSCE_PAT is configured
  release-vscode-extension:
    name: Release VS Code extension
    runs-on: blacksmith-32vcpu-ubuntu-2404
    needs: build-editor-extensions
    timeout-minutes: 15
    environment: vscode-marketplace
    permissions:
      contents: read
    env:
      VSCE_PAT: ${{ secrets.VSCE_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Cache pnpm store for VS Code extension tooling
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-vscode-release-${{ hashFiles('.node-version') }}-v1
          restore-keys: |
            ${{ runner.os }}-pnpm-vscode-release-

      - name: Download editor extension artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: editor-extensions
          path: editor-extension-artifacts

      - name: Skip publish when VSCE_PAT is absent
        if: env.VSCE_PAT == ''
        run: echo "VSCE_PAT is not configured; skipping VS Code Marketplace publish."

      - name: Publish VS Code extension
        if: env.VSCE_PAT != ''
        run: moon run --target native - -- editor-extension-artifacts/npm/vscode-vize/dist/vize.vsix < tools/moon/scripts/publish_vscode_extension.mbtx

  # Build release npm packages once and reuse them across publish jobs
  build-release-packages:
    name: Build release npm packages
    runs-on: blacksmith-32vcpu-ubuntu-2404
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Install package build dependencies
        run: moon run --target native - -- --filter './npm/vize...' --filter './npm/vite-plugin-vize...' --filter './npm/oxlint-plugin-vize...' --filter './npm/unplugin-vize...' --filter './npm/fresco...' --filter './npm/musea-mcp-server...' --filter './npm/vite-plugin-musea...' --filter './npm/rspack-vize-plugin...' --filter './npm/musea-nuxt...' --filter './npm/nuxt...' < tools/moon/scripts/github/vp_install.mbtx

      - name: Build release npm packages
        run: moon run --target native - -- vp run --filter './npm/vize' build -- vp run --filter './npm/vite-plugin-vize' build -- vp run --filter './npm/oxlint-plugin-vize' build -- vp run --filter './npm/unplugin-vize' build -- vp run --filter './npm/fresco' build -- vp run --filter './npm/musea-mcp-server' build -- vp run --filter './npm/vite-plugin-musea' build -- vp run --filter './npm/vite-plugin-musea' build:gallery -- vp run --filter './npm/rspack-vize-plugin' build -- vp run --filter './npm/musea-nuxt' build -- vp run --filter './npm/nuxt' build < tools/moon/scripts/github/run_many.mbtx

      - name: Upload vize CLI package artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-vize
          path: npm/vize
          if-no-files-found: error
          compression-level: 0

      - name: Upload Vite plugin artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-vite-plugin-vize
          path: npm/vite-plugin-vize
          if-no-files-found: error
          compression-level: 0

      - name: Upload Oxlint plugin artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-oxlint-plugin-vize
          path: npm/oxlint-plugin-vize
          if-no-files-found: error
          compression-level: 0

      - name: Upload unplugin artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-unplugin-vize
          path: npm/unplugin-vize
          if-no-files-found: error
          compression-level: 0

      - name: Upload Fresco artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-fresco
          path: npm/fresco
          if-no-files-found: error
          compression-level: 0

      - name: Upload Musea MCP server artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-musea-mcp-server
          path: npm/musea-mcp-server
          if-no-files-found: error
          compression-level: 0

      - name: Upload Musea Vite plugin artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-vite-plugin-musea
          path: npm/vite-plugin-musea
          if-no-files-found: error
          compression-level: 0

      - name: Upload Rspack plugin artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-rspack-vize-plugin
          path: npm/rspack-vize-plugin
          if-no-files-found: error
          compression-level: 0

      - name: Upload Musea Nuxt artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-musea-nuxt
          path: npm/musea-nuxt
          if-no-files-found: error
          compression-level: 0

      - name: Upload Nuxt artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-nuxt
          path: npm/nuxt
          if-no-files-found: error
          compression-level: 0

  # Build WASM once on Blacksmith; the npm publish job only attaches provenance.
  build-wasm-package:
    name: Build @vizejs/wasm package
    runs-on: blacksmith-32vcpu-ubuntu-2404
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: ./.github/actions/setup-moonbit

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache Rust
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          shared-key: release-wasm
          cache-workspace-crates: "true"
          save-if: "false"

      - name: Cache WASM package build
        id: cache-wasm
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: npm/vize-wasm
          key: ${{ runner.os }}-release-wasm-${{ hashFiles('crates/**/*.rs', 'Cargo.lock', 'npm/vize-wasm/package.json', 'npm/vize-wasm/index.js', 'npm/vize-wasm/index.d.ts', 'tools/moon/scripts/build_vize_wasm_package.mbtx') }}

      - name: Install wasm-bindgen-cli
        if: steps.cache-wasm.outputs.cache-hit != 'true'
        uses: taiki-e/install-action@184183c2401be73c3bf42c2e61268aa5855379c1 # v2
        with:
          tool: wasm-bindgen-cli@0.2.121

      - name: Build WASM
        if: steps.cache-wasm.outputs.cache-hit != 'true'
        run: moon run --target native - -- < tools/moon/scripts/build_vize_wasm_package.mbtx

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false

      - name: Smoke @vizejs/wasm package
        run: node tools/npm/smoke-wasm-package.mjs npm/vize-wasm

      - name: Upload WASM package artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-package-vize-wasm
          path: npm/vize-wasm
          if-no-files-found: error
          compression-level: 0

  # Build native bindings (vize-native + fresco-native) for all platforms
  build-native-all:
    name: Build Native - ${{ matrix.settings.target }}
    needs: plan-release-platforms
    runs-on: ${{ matrix.settings.host }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        settings: ${{ fromJSON(needs.plan-release-platforms.outputs.native_matrix) }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Enable long paths (Windows)
        if: runner.os == 'Windows'
        run: git config --system core.longpaths true

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Install dependencies
        run: vp install --frozen-lockfile --prefer-offline --filter './npm/vize-native...' --filter './npm/fresco-native...'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          targets: ${{ matrix.settings.target }}

      - name: Install Rust target
        run: rustup target add ${{ matrix.settings.target }}

      - name: Setup Wild linker (Linux)
        if: runner.os == 'Linux'
        uses: wild-linker/action@0bbbfa5df4380cab8e63cb8505a1ce65e1d10203 # v0.9.0
        with:
          wild-version: "0.9.0"

      - name: Cache Rust
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          shared-key: release-native-${{ matrix.settings.target }}
          cache-workspace-crates: "true"
          save-if: "false"

      - name: Tune Windows release profile
        if: runner.os == 'Windows'
        shell: bash
        run: |
          echo "CARGO_PROFILE_RELEASE_LTO=thin" >> "$GITHUB_ENV"
          echo "CARGO_PROFILE_RELEASE_CODEGEN_UNITS=16" >> "$GITHUB_ENV"

      - name: Setup Zig
        if: matrix.settings.cross_compile
        uses: goto-bus-stop/setup-zig@abea47f85e598557f500fa1fd2ab7464fcb39406 # v2

      - name: Clean native build outputs
        shell: bash
        run: moon run --target native - -- npm/vize-native npm/fresco-native < tools/moon/scripts/github/clean_node_binaries.mbtx

      - name: Build vize-native
        shell: bash
        run: moon run --target native - -- npm/vize-native ../../crates/vize_vitrine/Cargo.toml vize_vitrine ${{ matrix.settings.target }} ${{ matrix.settings.cross_compile == true && '--cross-compile' || '' }} < tools/moon/scripts/github/build_napi_package.mbtx

      - name: Collect vize-native artifact
        shell: bash
        run: moon run --target native - -- npm/vize-native .artifacts/vize-native vize-native < tools/moon/scripts/github/collect_native_artifacts.mbtx

      - name: Upload vize-native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bindings-${{ matrix.settings.target }}
          path: .artifacts/vize-native/*.node
          if-no-files-found: error
          compression-level: 0

      - name: Build fresco-native
        shell: bash
        run: moon run --target native - -- npm/fresco-native ../../crates/vize_fresco/Cargo.toml vize_fresco ${{ matrix.settings.target }} ${{ matrix.settings.cross_compile == true && '--cross-compile' || '' }} < tools/moon/scripts/github/build_napi_package.mbtx

      - name: Collect fresco-native artifact
        shell: bash
        run: moon run --target native - -- npm/fresco-native .artifacts/fresco-native fresco-native < tools/moon/scripts/github/collect_native_artifacts.mbtx

      - name: Upload fresco-native artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: fresco-bindings-${{ matrix.settings.target }}
          path: .artifacts/fresco-native/*.node
          if-no-files-found: error
          compression-level: 0

  smoke-release-packages:
    name: Smoke release npm package installs
    runs-on: blacksmith-32vcpu-ubuntu-2404
    needs: [plan-release-platforms, build-release-packages, build-native-all]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Install native package tooling dependencies
        run: vp install --frozen-lockfile --prefer-offline --filter './npm/vize-native...' --filter './npm/fresco-native...'

      - name: Download vize CLI package artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-vize
          path: npm/vize

      - name: Download Vite plugin artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-vite-plugin-vize
          path: npm/vite-plugin-vize

      - name: Download Oxlint plugin artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-oxlint-plugin-vize
          path: npm/oxlint-plugin-vize

      - name: Download unplugin artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-unplugin-vize
          path: npm/unplugin-vize

      - name: Download Fresco artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-fresco
          path: npm/fresco

      - name: Download Musea MCP server artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-musea-mcp-server
          path: npm/musea-mcp-server

      - name: Download Musea Vite plugin artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-vite-plugin-musea
          path: npm/vite-plugin-musea

      - name: Download Rspack plugin artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-rspack-vize-plugin
          path: npm/rspack-vize-plugin

      - name: Download Musea Nuxt artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-musea-nuxt
          path: npm/musea-nuxt

      - name: Download Nuxt artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-nuxt
          path: npm/nuxt

      - name: Download vize native artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: bindings-*
          path: npm/vize-native/artifacts

      - name: Download fresco native artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: fresco-bindings-*
          path: npm/fresco-native/artifacts

      - name: Apply slow platform release cadence
        run: node tools/github/release-platforms.mjs apply-cadence "${{ github.ref_name }}" npm/vize-native/package.json npm/oxlint-plugin-vize/package.json

      - name: Prepare native package tarballs
        working-directory: npm/vize-native
        run: |
          vp exec napi create-npm-dirs
          vp exec napi artifacts --output-dir artifacts
          vp exec napi pre-publish -t npm --no-gh-release --skip-optional-publish

      - name: Prepare Fresco native package tarball
        run: |
          moon run --target native - -- npm/fresco-native < tools/moon/scripts/github/clean_node_binaries.mbtx
          moon run --target native - -- npm/fresco-native/artifacts npm/fresco-native fresco-native < tools/moon/scripts/github/collect_native_artifacts.mbtx

      - name: Smoke install release package tarballs
        run: |
          node tools/npm/smoke-release-install.mjs --prepare-manifests --runtime-checks \
            npm/vize-native npm/vize-native/npm/* \
            npm/fresco-native \
            npm/vize \
            npm/vite-plugin-vize \
            npm/oxlint-plugin-vize \
            npm/unplugin-vize \
            npm/fresco \
            npm/musea-mcp-server \
            npm/vite-plugin-musea \
            npm/rspack-vize-plugin \
            npm/musea-nuxt \
            npm/nuxt

  # npm Trusted Publishing/provenance currently rejects self-hosted runners.
  # Keep the publish edge on GitHub-hosted runners; build and smoke jobs stay on Blacksmith.
  # Publish native packages to npm (using Trusted Publishing / OIDC)
  release-npm-native:
    name: Release @vizejs/native to npm
    runs-on: ubuntu-24.04
    needs: [plan-release-platforms, build-native-all]
    timeout-minutes: 30
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Install dependencies
        run: vp install --frozen-lockfile --prefer-offline --filter './npm/vize-native...'

      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: bindings-*
          path: npm/vize-native/artifacts

      - name: Apply slow platform release cadence
        run: node tools/github/release-platforms.mjs apply-cadence "${{ github.ref_name }}" npm/vize-native/package.json

      - name: Create npm platform directories
        working-directory: npm/vize-native
        run: vp exec napi create-npm-dirs

      - name: Move artifacts
        working-directory: npm/vize-native
        run: vp exec napi artifacts --output-dir artifacts

      - name: Prepare for publish
        working-directory: npm/vize-native
        run: vp exec napi pre-publish -t npm --no-gh-release --skip-optional-publish

      - name: List packages
        run: ls -R npm/vize-native/npm/

      - name: Smoke install native package tarballs
        run: node tools/npm/smoke-release-install.mjs --prepare-manifests --runtime-checks npm/vize-native npm/vize-native/npm/*

      - name: Publish platform packages
        run: moon run --target native - -- npm/vize-native/npm --provenance < tools/moon/scripts/publish_npm_package_dirs.mbtx

      - name: Publish
        run: moon run --target native - -- npm/vize-native --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Publish fresco native packages to npm (using Trusted Publishing / OIDC)
  release-npm-fresco-native:
    name: Release @vizejs/fresco-native to npm
    runs-on: ubuntu-24.04
    needs: build-native-all
    timeout-minutes: 20
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          cache: true
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Install dependencies
        run: vp install --frozen-lockfile --prefer-offline --filter './npm/fresco-native...'

      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: fresco-bindings-*
          path: npm/fresco-native/artifacts

      - name: Clean bundled native binaries
        run: moon run --target native - -- npm/fresco-native < tools/moon/scripts/github/clean_node_binaries.mbtx

      - name: Stage bundled native binaries
        run: moon run --target native - -- npm/fresco-native/artifacts npm/fresco-native fresco-native < tools/moon/scripts/github/collect_native_artifacts.mbtx

      - name: Smoke install Fresco native package tarball
        run: node tools/npm/smoke-release-install.mjs --prepare-manifests npm/fresco-native

      - name: Publish
        run: moon run --target native - -- npm/fresco-native --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Publish WASM package (using Trusted Publishing / OIDC)
  release-npm-wasm:
    name: Release @vizejs/wasm to npm
    runs-on: ubuntu-24.04
    needs: build-wasm-package
    timeout-minutes: 30
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: ./.github/actions/setup-moonbit

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false

      - name: Download prebuilt WASM package
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-vize-wasm
          path: npm/vize-wasm

      - name: Smoke @vizejs/wasm package
        run: node tools/npm/smoke-wasm-package.mjs npm/vize-wasm

      - name: Smoke install WASM package tarball
        run: node tools/npm/smoke-release-install.mjs npm/vize-wasm

      - name: Publish @vizejs/wasm
        run: moon run --target native - -- npm/vize-wasm --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish Vite plugin (using Trusted Publishing / OIDC)
  release-npm-vite-plugin:
    name: Release @vizejs/vite-plugin to npm
    runs-on: ubuntu-24.04
    needs: [build-release-packages, release-npm-native, release-npm-cli, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-vite-plugin-vize
          path: npm/vite-plugin-vize

      - name: Publish @vizejs/vite-plugin
        run: moon run --target native - -- npm/vite-plugin-vize --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish Oxlint plugin package (using Trusted Publishing / OIDC)
  release-npm-oxlint-plugin:
    name: Release oxlint-plugin-vize to npm
    runs-on: ubuntu-24.04
    needs:
      [plan-release-platforms, build-release-packages, release-npm-native, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-oxlint-plugin-vize
          path: npm/oxlint-plugin-vize

      - name: Inject native binding package versions
        run: moon run --target native - -- npm/oxlint-plugin-vize/package.json npm/vize-native/package.json --print < tools/moon/scripts/inject_native_optional_deps.mbtx

      - name: Apply slow platform release cadence
        run: node tools/github/release-platforms.mjs apply-cadence "${{ github.ref_name }}" npm/oxlint-plugin-vize/package.json

      - name: Publish oxlint-plugin-vize
        run: moon run --target native - -- npm/oxlint-plugin-vize --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish unplugin package (using Trusted Publishing / OIDC)
  release-npm-unplugin:
    name: Release @vizejs/unplugin to npm
    runs-on: ubuntu-24.04
    needs: [build-release-packages, release-npm-native, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-unplugin-vize
          path: npm/unplugin-vize

      - name: Publish @vizejs/unplugin
        run: moon run --target native - -- npm/unplugin-vize --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish Fresco package (using Trusted Publishing / OIDC)
  release-npm-fresco:
    name: Release @vizejs/fresco to npm
    runs-on: ubuntu-24.04
    needs: [build-release-packages, release-npm-fresco-native, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-fresco
          path: npm/fresco

      - name: Publish @vizejs/fresco
        run: moon run --target native - -- npm/fresco --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish Musea MCP server (using Trusted Publishing / OIDC)
  release-npm-musea-mcp-server:
    name: Release @vizejs/musea-mcp-server to npm
    runs-on: ubuntu-24.04
    needs: [build-release-packages, release-npm-native, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-musea-mcp-server
          path: npm/musea-mcp-server

      - name: Publish @vizejs/musea-mcp-server
        run: moon run --target native - -- npm/musea-mcp-server --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish Vite plugin for Musea (using Trusted Publishing / OIDC)
  release-npm-vite-plugin-musea:
    name: Release @vizejs/vite-plugin-musea to npm
    runs-on: ubuntu-24.04
    needs: [build-release-packages, release-npm-native, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-vite-plugin-musea
          path: npm/vite-plugin-musea

      - name: Publish @vizejs/vite-plugin-musea
        run: moon run --target native - -- npm/vite-plugin-musea --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish Rspack plugin (using Trusted Publishing / OIDC)
  release-npm-rspack-plugin:
    name: Release @vizejs/rspack-plugin to npm
    runs-on: ubuntu-24.04
    needs: [build-release-packages, release-npm-native, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-rspack-vize-plugin
          path: npm/rspack-vize-plugin

      - name: Publish @vizejs/rspack-plugin
        run: moon run --target native - -- npm/rspack-vize-plugin --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish Musea Nuxt (using Trusted Publishing / OIDC)
  release-npm-musea-nuxt:
    name: Release @vizejs/musea-nuxt to npm
    runs-on: ubuntu-24.04
    needs: [build-release-packages, release-npm-native, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-musea-nuxt
          path: npm/musea-nuxt

      - name: Publish @vizejs/musea-nuxt
        run: moon run --target native - -- npm/musea-nuxt --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Build and publish @vizejs/nuxt (using Trusted Publishing / OIDC)
  release-npm-nuxt:
    name: Release @vizejs/nuxt to npm
    runs-on: ubuntu-24.04
    needs:
      [
        build-release-packages,
        release-npm-vite-plugin,
        release-npm-vite-plugin-musea,
        release-npm-musea-nuxt,
        release-npm-cli,
        smoke-release-packages,
      ]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-nuxt
          path: npm/nuxt

      - name: Publish @vizejs/nuxt
        run: moon run --target native - -- npm/nuxt --provenance < tools/moon/scripts/publish_npm_package.mbtx

  # Publish crates to crates.io (using Trusted Publishing)
  release-crates:
    name: Release crates to crates.io
    runs-on: blacksmith-32vcpu-ubuntu-2404
    timeout-minutes: 30
    environment: crates-io
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: ./.github/actions/setup-moonbit

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          shared-key: release-crates
          cache-workspace-crates: "true"
          save-if: "false"

      - name: Get crates.io token via Trusted Publishing
        id: crates-io-auth
        uses: rust-lang/crates-io-auth-action@b7e9a28eded4986ec6b1fa40eeee8f8f165559ec # v1

      - name: Publish to crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-io-auth.outputs.token }}
        run: moon run --target native - -- < tools/moon/scripts/publish_crates.mbtx

  # Create GitHub Release with CLI binaries
  create-github-release:
    name: Create GitHub Release
    runs-on: blacksmith-32vcpu-ubuntu-2404
    needs:
      - build-cli
      - release-vscode-extension
      - release-npm-native
      - release-npm-fresco-native
      - release-npm-wasm
      - smoke-release-packages
      - release-npm-cli
      - release-npm-vite-plugin
      - release-npm-oxlint-plugin
      - release-npm-unplugin
      - release-npm-fresco
      - release-npm-musea-mcp-server
      - release-npm-vite-plugin-musea
      - release-npm-rspack-plugin
      - release-npm-musea-nuxt
      - release-npm-nuxt
      - release-crates
    timeout-minutes: 20
    # `id-token: write` is required by sigstore/cosign-installer to mint the
    # keyless signing certificate, and by anchore/sbom-action when it uploads
    # the SBOM as a release asset.
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Download CLI artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: cli-*
          path: cli-artifacts
          merge-multiple: true

      - name: Download editor extension artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: editor-extensions
          path: editor-extension-artifacts

      - name: List CLI artifacts
        run: ls -la cli-artifacts/

      - name: List editor extension artifacts
        run: find editor-extension-artifacts -maxdepth 5 -type f | sort

      # SBOM: generate one SBOM per source-of-truth artifact set so adopters
      # can audit transitive dependencies for a release tag without rebuilding.
      # CycloneDX 1.5 JSON is the lingua franca for SCA tooling (Snyk, Trivy,
      # Grype, GitHub Dependency Graph). SPDX is generated by the same action
      # when adopters need an OSS-license-focused view.
      - name: Generate workspace SBOM (CycloneDX)
        uses: anchore/sbom-action@61119d458adab75f756bc0b9e4bde25725f86a7a # v0.17.8
        with:
          artifact-name: vize-${{ github.ref_name }}-cyclonedx.sbom.json
          format: cyclonedx-json
          output-file: vize-${{ github.ref_name }}-cyclonedx.sbom.json
          upload-artifact: false

      - name: Generate workspace SBOM (SPDX)
        uses: anchore/sbom-action@61119d458adab75f756bc0b9e4bde25725f86a7a # v0.17.8
        with:
          artifact-name: vize-${{ github.ref_name }}-spdx.sbom.json
          format: spdx-json
          output-file: vize-${{ github.ref_name }}-spdx.sbom.json
          upload-artifact: false

      # Cosign keyless signing via Sigstore. The OIDC token issued to the
      # workflow stands in for a long-lived private key, and the resulting
      # certificate + signature live in the public Rekor transparency log.
      # Each .tar.gz / .zip / .vsix gets a detached `.sig` and `.pem` so
      # adopters can verify with `cosign verify-blob --certificate-identity ...`.
      - name: Install cosign
        uses: sigstore/cosign-installer@dc72c7d5c4d10cd6bcb8cf6e3fd625a9e5e537da # v3.7.0
        with:
          cosign-release: v2.4.1

      # SHA-256 sums are the simplest integrity check downstream tools
      # (the VS Code extension's pre-execution download) can verify
      # without pulling in cosign or sigstore-verify libraries. Cosign
      # remains the source of truth via the bundle below; the .sha256
      # files are an easy-to-verify hash pin. (#969)
      - name: Generate SHA-256 checksums for release artifacts
        run: |
          for artifact in \
            cli-artifacts/*.tar.gz \
            cli-artifacts/*.zip \
            editor-extension-artifacts/npm/vscode-vize/dist/*.vsix \
            editor-extension-artifacts/*.tar.gz; do
            if [ -f "$artifact" ]; then
              echo "Hashing $artifact"
              ( cd "$(dirname "$artifact")" && sha256sum "$(basename "$artifact")" ) > "${artifact}.sha256"
            fi
          done

      - name: Sign release artifacts (keyless)
        env:
          COSIGN_YES: "true"
        run: |
          for artifact in \
            cli-artifacts/*.tar.gz \
            cli-artifacts/*.zip \
            editor-extension-artifacts/npm/vscode-vize/dist/*.vsix \
            editor-extension-artifacts/*.tar.gz \
            vize-${{ github.ref_name }}-cyclonedx.sbom.json \
            vize-${{ github.ref_name }}-spdx.sbom.json; do
            if [ -f "$artifact" ]; then
              echo "Signing $artifact"
              cosign sign-blob --yes \
                --bundle "${artifact}.cosign.bundle" \
                --output-certificate "${artifact}.pem" \
                --output-signature "${artifact}.sig" \
                "$artifact"
            fi
          done

      - name: Create Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          generate_release_notes: true
          draft: false
          overwrite_files: true
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}
          files: |
            cli-artifacts/*.tar.gz
            cli-artifacts/*.zip
            editor-extension-artifacts/npm/vscode-vize/dist/*.vsix
            editor-extension-artifacts/*.tar.gz
            vize-${{ github.ref_name }}-cyclonedx.sbom.json
            vize-${{ github.ref_name }}-spdx.sbom.json
            cli-artifacts/*.sha256
            cli-artifacts/*.sig
            cli-artifacts/*.pem
            cli-artifacts/*.cosign.bundle
            editor-extension-artifacts/**/*.sha256
            editor-extension-artifacts/**/*.sig
            editor-extension-artifacts/**/*.pem
            editor-extension-artifacts/**/*.cosign.bundle
            vize-${{ github.ref_name }}-*.sig
            vize-${{ github.ref_name }}-*.pem
            vize-${{ github.ref_name }}-*.cosign.bundle

  # Publish vize CLI npm package (using Trusted Publishing / OIDC)
  # Native bindings are distributed via @vizejs/native-* optionalDependencies (NAPI-RS)
  release-npm-cli:
    name: Release vize CLI to npm
    runs-on: ubuntu-24.04
    needs: [build-release-packages, release-npm-native, smoke-release-packages]
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Vite+ and Node.js
        uses: voidzero-dev/setup-vp@ca1c46663915d6c1042ae23bd39ab85718bfb0fa # v1
        with:
          node-version-file: ".node-version"
          run-install: false
      - uses: ./.github/actions/setup-moonbit

      - name: Download prebuilt packages
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-package-vize
          path: npm/vize

      - name: Inject native binding package versions
        run: moon run --target native - -- npm/vize/package.json --print < tools/moon/scripts/inject_native_optional_deps.mbtx

      - name: Publish vize CLI package
        run: moon run --target native - -- npm/vize --provenance < tools/moon/scripts/publish_npm_package.mbtx
